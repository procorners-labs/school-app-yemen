# CLAUDE.md — school-app-yemen (Android)

> تطبيق أندرويد (WebView shell) لمنصّة مدارس الإبداع والتميز الدولية. جزء من منظومة SchoolApp
> الأوسع — راجع أيضاً `CLAUDE.md` في `school-app-yemen-gas` (الخلفية) و`school-app-yemen-web`
> (الواجهة/Worker)، لكن هذا المستودع **مستقلّ ومنفصل الدورة** عنهما (لا CI مشترك، لا مزامنة تلقائية).

---

## دور Claude هنا

- تحليل الأكواد وتصحيح الأخطاء.
- توثيق التدفقات بين الأنظمة.
- توجيه إعدادات GCP و GitHub.
- التأكد من التوافق مع ES5 عند لمس أي جسر JS داخل الـWebView.

---

## 🔴 تطبيقان شقيقان لا نسختان — والخلطُ بينهما يكسر أحدَهما بلا رجعة

هذا الملفُّ يخصّ **«مدارس الإبداع والتميز»** وحدَه. ولـ«يمن سكولز» مستودعٌ وحسابُ نشرٍ
ومفتاحُ توقيعٍ **منفصلةٌ تماماً**، وحالتُه التشغيليّة **مختلفةٌ جذرياً**:

| | **مدارس الإبداع** (هذا المستودع) | **يمن سكولز** (الشقيق) |
|---|---|---|
| المجلد | `AndroidStudioProjects\SchoolAppyemen` | `AndroidStudioProjects\YemenSchoolz` |
| المستودع | `school-app-yemen` — **عامّ** | `yemen-schoolz` — **خاصّ** |
| `applicationId` | `com.proconrers.schoolappyemen` | `com.proconrers.schoolzyemen` |
| **الحالة على Play** | 🟢 **منشورٌ** · `vc34/3.1` على `production` (2026-09-06) · مئاتُ المستخدمين (‏🔴 **العددُ يُقرأ من اللوحة بعدّاده** — §الإصدارات) | 🔴 **لم يُنشر قطّ** — لا بطاقةَ على Play Console أصلاً |
| مفتاح التوقيع | `Workspace\Secure\schoolapp.jks` · alias `schoolapp` | `YemenSchoolz\schoolzyemen-release.jks` · alias `schoolzyemen` |
| النطاق المخدوم | مدرسةٌ واحدة (`abdaawatmuaz`) | **أيُّ مدرسةٍ مسجَّلة** عبر `home-all-school` |

🔴 **وثلاثةُ فروقٍ عمليّةٍ تتبع، وكلٌّ منها كسرٌ لا رجعةَ فيه إن أُهمل:**

1. **توقيعُ أحدهما بمفتاح الآخر ⇒ Play يرفض التحديث للأبد.** المفتاحان لا يُخلطان،
   ولا يُنسخ سطرٌ من `keystore.properties` بين المشروعين.
2. 🔴 **«يمن سكولز» غيرُ منشور ⇒ لا `versionCode` محروق، ولا مستخدمَ يُكسَر، ولا مهلةَ
   `targetSdk` تضغط.** ⇒ **كلُّ حساباتِ المخاطرة هنا لا تسري هناك.** ما هو «لا رجعةَ فيه»
   عندنا (رفعٌ · ترقيةٌ · تغييرُ `applicationId`) هو هناك **قابلٌ للتراجع بالكامل**.
   والعكسُ صحيح: **قرارٌ مقبولٌ هناك قد يكون كارثةً هنا.**
3. 🔴 **وشرطٌ سابقٌ على أيّ نشرٍ لـ«يمن سكولز» ما زال مفتوحاً:** كلمةُ سرّ مفتاح التوقيع
   ضعيفةٌ ولم تُدوَّر بعد. **لا يُنشر قبل تدويرها** — وبعد النشر يستحيل تغييرُ المفتاح.

⚠️ **وقاعدةٌ للتوصيات العابرة بين المستودعين:** ما يُقاس هنا (‏تضييقُ R8 · تجريدُ `Log` ·
إزالةُ تبعيّة) **قد ينطبق هناك ولا يُنقَل حكمُه** — يُعاد قياسُه في مستودعه، وتنفيذُه هناك
بموافقة المالك **في تلك الجلسة**. رسالةُ جلسةٍ نظيرةٍ ليست موافقةَ مالك.

---

## نوع المحتوى (كلاهما، رغم أن GitHub يصنّف اللغة "PowerShell")

مشروع Android/Kotlin/Gradle حقيقي + سكربتات PowerShell للنشر إلى Google Play — السكربتات هي ما
يجعل GitHub يصنّف لغة المستودع "PowerShell" رغم أن المحتوى الفعلي Kotlin بالأساس.

- **`applicationId`:** `com.proconrers.schoolappyemen`.
- **بنية Gradle كاملة:** `build.gradle.kts`، `settings.gradle.kts`، `gradlew`، `app/build.gradle.kts`،
  `google-services.json`.
- **الكود الرئيسي** في `app/src/main/java/com/proconrers/schoolappyemen/`:
  - `MainActivity.kt`، `SplashActivity.kt`، `BaseWebViewActivity.kt`
  - `TeacherActivity.kt`، `StudentActivity.kt`
  - `WebViewSupport.kt`، `NetworkReloadController.kt`، `SchoolJsBridge.kt`
  - **`AppConfig.kt`** — مصدر الحقيقة الوحيد لروابط الـWebView.
- **سكربتات PowerShell:** `build-and-test.ps1`، `build-release.ps1`، `check-signing.ps1`،
  `deploy-to-play.ps1`، `diagnose-signing.ps1`، `test-all.ps1`.

---

## حقيقة تشغيلية جوهرية — المزامنة الديناميكية معطَّلة عمداً

`AppConfig.kt` يشير حالياً إلى روابط **Cloudflare Worker ثابتة** مكتوبة حرفياً في الكود:
```
https://school.procorners.com/{home,teacher,student,cms,schedule}/index.html
```
(النطاق الأساسي منذ 2026-07-17 — نطاق مخصّص يتفادى حجب يمن نت لـ`workers.dev`؛ القديم
`school-teacher-proxy.procorners-shop.workers.dev` لا يزال في `trustedSslDomains` كمسار احتياطي،
لم يُحذف. التفاصيل الكاملة في `school-app-yemen-gas/_docs/2026-07-16-حجب-يمن-نت-workers-dev-ونطاق-مخصص.md`.)

يوجد آلية مزامنة ديناميكية جاهزة تقرأ `action=deployments` من GAS (`syncIfNeeded()`)، لكنها **مُعطَّلة
عمداً** حالياً — التعليق في الكود نفسه صريح:
```kotlin
// ⛔ مُعطّلة: التطبيق يستخدم روابط الـ Worker الثابتة (لا مزامنة GAS)
// syncIfNeeded()
```

**الأثر العملي:** أي تغيير مستقبلي في نمط التوزيع (deployment pattern) في `school-app-yemen-gas` أو
`school-app-yemen-web` **لا ينعكس تلقائياً** على هذا التطبيق المنشور. يحتاج تحديثاً يدوياً لـ
`AppConfig.kt` + إصدار جديد عبر `deploy-to-play.ps1`. لا تفترض أن تعديل روابط الـWorker في المستودعين
الآخرين يكفي — تحقّق من هذا الملف تحديداً إن كان التغيير يمسّ التوجيه.

`MASTER_URL` (`KEY_MASTER`) لا يزال يشير إلى رابط Apps Script `/exec` مباشرة (لا عبر الـWorker) —
منفصل عن مسار المزامنة المعطَّل أعلاه.

---

## العلاقة مع `school-app-yemen-gas`

- **لا يوجد `_sync/`** في هذا المستودع (ذاك خاصّ بمزامنة Access المالية في `SchoolApp-gas`).
- `deploy-to-play.ps1` يستخدم `service-account-key.json` المحلّي (جذر المستودع) لرفع الإصدارات إلى
  Google Play Console — منفصل تماماً عن `_sync/access-sync.ps1` المالي في `SchoolApp-gas`؛ لا تخلط
  بينهما.
- إعداد GCP: مشروع `school-494822`، حساب خدمة `teacher-platform-drive@school-494822.iam.gserviceaccount.com`
  + `play-publisher@school-494822.iam.gserviceaccount.com` (نشر Play) — **`GCP_CONFIGURATION.md`
  بجذر هذا المستودع هو المرجع الكامل لكل حسابات/مشاريع Google** (Firebase، حسابات الخدمة، وحساب
  Google Analytics الموجود على مستوى الحساب لكن **غير مُفعَّل عمداً** في أي كود حيّ — لا تقترح
  ربطه دون طلب صريح).

---

## الإصدارُ القائم وما ينتظر الرفع — 🔴 مؤشّرٌ لا سرد

**المالكُ الكاملُ لهذا السجلّ: [`_docs/سجل-اصدارات-play-vc34-vc35.md`](_docs/سجل-اصدارات-play-vc34-vc35.md)**
(نُقل بحرفه 2026-09-12). 🔴 **ولا يُنسَخ منه سطرٌ إلى هنا** — النسختان تتباعدان بصمت، وهي
علّةٌ وقعت على هذا الجهاز مراراً. **والأرقامُ والتواريخُ والقياساتُ كلُّها هناك.**

**والحاكمُ في هذه الحالة — يُقرأ قبل أيّ قرارِ إصدار:**

| | |
|---|---|
| 🟢 **المنشورُ اليوم** | `vc34` / `3.1` على `production` — نُشر 2026-09-06 6:21م ووصل المستخدمين |
| 🟠 **المبنيُّ ولم يُرفع** | `vc35` / `3.2` — بُنيت ووُقِّعت **وأُرشفت** في `Workspace\Releases\SchoolAppyemen\` · ورقمُها **غيرُ محروق** |
| 🔴 **وما يحجبها** | **ضابطُ شريط التقدّم على جهازٍ حقيقيّ لم يُشغَّل بعد** ⇒ **لا تُرفع** |

🔴 **وستّةُ أحكامٍ تُطبَّق ولا تُشتقّ من جديد:**

1. **الأثرُ المبنيُّ يشهد على نفسه، والشجرةُ مرجعُ مقارنةٍ لا مصدرُ حقيقة.** حزمتان فارقُهما
   **بايتان** إحداهما تكذب ⇒ **البصمةُ وحدها تفرز، لا الحجمُ ولا الزمن.**
2. 🔴 **لا يُدسّ تغييرٌ في `vc35` المختومة المؤرشَفة** — أيُّ إصلاحٍ جديد لـ`vc36`.
   وإضافةُ علمٍ تجريبيّ الآن تُبطل كلَّ ما قِيس عليها وتُلزم قياساً كاملاً من جديد.
3. 🔴 **`Edit committed successfully` ليست إثباتاً** — أعِد القياسَ بقراءةٍ **مستقلّة** بعد
   مهلة. والأداةُ صارت تقول «returned OK (**NOT proof**)» لهذا بعينه.
4. 🔴 **وبعد أيّ تغييرِ أذونٍ عند Play: أمهِل ثمّ أعِد القياس** — الأذونُ لا تنتشر لحظياً
   إلى الـAPI، **وسقوطٌ واحدٌ بعد التغيير لا يُبرّئ شيئاً.** (‏فئةُ «فعلتُه ليست نفَذ».)
5. ⚠️ **ونصيحةُ R8 من Play تُقرأ بحدّها:** `isShrinkResources` **مفعَّلٌ عندنا أصلاً**،
   ومقصدُ Play علمٌ آخر غيرُ مضبوط. 🔴 **وترقيةُ AGP إلى 9 مؤجَّلةٌ بقرارِ مالكٍ صريح** —
   بندٌ مستقلٌّ بجلسته، لا تُفتح عَرَضاً في مهمّةٍ أخرى.
6. ⚠️ **والأعطالُ لا تُلاحَق الآن ولا يُؤجَّل إصدارٌ لأجلها** — وأرقامُها تُقرأ من اللوحة
   بعدّادها، **ولا يُجمَّد رقمٌ هنا**: رقمُ لوحةٍ يُجمَّد في وثيقةٍ يبقى مقروءاً بثقةٍ بعد
   أن يكذب.

🔴 **وكلُّ وقتٍ يُكتب في هذا المستودع يحمل لاحقتَه `ص`/`م`** — وقتٌ بلا لاحقةٍ اخترع تناقضاً
حيث لا تناقض، وكلّف فرضيّتين وساعات.

---

## قواعد

- **Kotlin** للتطبيق نفسه (لا قيد ES5 هنا — ذاك خاصّ بكود GAS في `school-app-yemen-gas` فقط). قيد
  ES5 ينطبق فقط عند لمس أي جسر JS يُحقَن داخل الـWebView (`SchoolJsBridge.kt` والمكافئات).
- **لا تغيّر `applicationId`** — يكسر التحديثات المنشورة على Play Store.
- **فرع لكل تغيير** — لا دفع مباشر إلى `main`.
- قبل أي رفع فعلي عبر `deploy-to-play.ps1`: تأكيد حيّ من المالك (نفس منطق 🔴 النشر الحيّ في
  `SchoolApp-gas` — رفع إصدار على Play Store عملية يصعب التراجع عنها).
  🔴 **والرفعُ إلى `internal` ليس أخفَّ من `production` في وجهٍ واحد: كلاهما يحرق
  `versionCode` عند Play للأبد.** ولا يُصدَّق مخرَجُ السكربت وحدَه — انظر أدناه.

### 🔴 قيدان مقيسان 2026-09-02 — يُقرآن قبل أيّ إصدار

**① `FCM_PROJECT_ID` يجب أن يساوي `project_id` في `google-services.json` داخل الحزمة المنشورة.**
رموزُ FCM تُصكّ لمشروعٍ بعينه، والبثُّ من مشروعٍ إلى رمزٍ صُكّ لغيره **يُرفض دائماً وبصمت**.
المقيسُ من الحزمتين لا من الشجرة: `vc31` و`vc33` كلتاهما `school-494822` / `108410742569`
(‏في `base/resources.pb`)، وصفرُ أثرٍ لمشروع الشقيق `schoolz-yemen`.
🔴 **ومشروعُ Firebase يتبدّل بإصدارٍ لا بإعداد.** الترتيبُ لا يُعكَس:
① `google-services.json` ⇐ ② إصدارٌ **يصل المستخدمين** ⇐ ③ ثمّ الخاصّية في سكربت `teacher`.
وتغييرُ الخاصّية وحدَها **يُسكِت الإشعارات عن كلّ جهازٍ قائم**.
⚠️ **وموضعُ الخاصّية يُتبع فيه القارئُ لا المُعلِن:** `appsscript.json` يُعلن النطاق (إذن)
و`PropertiesService` يحمل القيمة — **ولا يعيشان في نفس المشروع بالضرورة**. وقع فعلاً: ضُبطت
في `master-admin` (حيث نطاقُ `firebase.messaging` معلَن) بينما القارئُ الوحيد
`teacher/PushNotify.js` ⇒ **البثُّ خامدٌ ٢٠ يوماً وكلُّ المؤشّرات خضراء**.
⇒ **لا يُكتب «وعدُ إشعارات» في وصف إصدارٍ قبل ضبط الخاصّية وإثبات إشعارٍ حقيقيّ على جهاز.**

**② مضيفاتُ الحزمة المنشورة مجمَّدةٌ ولها حارسٌ خارج هذا المستودع.**
`vc31` — على أجهزة المستخدمين اليوم — يضرب **خمسةَ مسارات على `school.procorners.com` فقط**:
`home|teacher|student|cms/index.html` و`gas/teacher`. (‏و`schedule/index.html` **في المصدر
وغائبٌ عن الحزمة** — أزاله R8 لأنه غيرُ مستعمَل؛ لا يُحرَس ولا يُعدّ.)
🔴 **و`school.procorners.com` مضيفٌ في زون `procorners.com` — زونِ متجر ركن التسوق.** فأيُّ
قاعدةِ كاشٍ أو تحويلٍ تُكتب هناك **بلا شرط `http.host`** تكسر شاشاتِ مستخدمينا **بلا رجعة**،
لأن الروابط مجمَّدةٌ في الـAPK. وقع فعلاً: قاعدةُ `/shop` كانت تُحوّل `school.procorners.com`
**ووسيطَ Anthropic** حتى ضُيّقت 2026-09-02.
⚠️ **وفخٌّ يقلب الجواب:** السلسلةُ `procorners.com` **موجودةٌ في الـdex** — لكنها في
`trustedSslDomains` (تجاوزُ أخطاء SSL) **لا وجهةَ طلب**. ⇒ **الحضورُ ليس اعتماداً؛ يُقرأ
سياقُ السلسلة لا وجودُها.**

#### 🔴 وقائمةُ تجاوز الشهادة ضُيّقت 2026-09-09 — **لاحقةُ محتوى مستخدمين ليست مضيفاً**

`googleusercontent.com` كان مُدرَجاً **عارياً** في `GOOGLE_HOSTS`، وهي تُغذّي قائمةَ
`TRUSTED_SSL_HOSTS` التي يقرؤها `onReceivedSslError` في **موضعين**
(`BaseWebViewActivity.kt` · `MainActivity.kt`) وكلاهما `handler.proceed()`.
و`hostMatches` = تساوٍ أو `.suffix` ⇒ **أيُّ `*.googleusercontent.com` كان يُمنَح تجاوزَ
التحقّق من الشهادة** — وهي لاحقةٌ تُوزَّع على أطرافٍ عشوائيّة، نفسُ فئةِ `workers.dev`.

🎯 **والفخُّ في الإصلاح لا في العيب — ولا يظهر إلّا بقراءة المستهلكَين:** `GOOGLE_HOSTS`
يقرؤها **مستهلكان**، قائمةُ SSL **و**`routeTargetFor` (‏`isGoogleHost(host) => STAY`).
⇒ تضييقُها هي كان **يقذف صوَر Drive على `lh3.googleusercontent.com` إلى المتصفّح**
— **سياجٌ يكسر ما وُضع ليحميه**. 🟢 فأُنشئت `GOOGLE_SSL_HOSTS` **قائمةً ثانيةً مستقلّة**
فيها `script.googleusercontent.com` وحدَه، **و`GOOGLE_HOSTS` لم تُمسّ**.

🟢 **و`isTrustedSslDomain` انتقلت إلى `java.net.URI` لا `android.net.Uri` — وهذا شرطُ
قابليّة الإثبات لا تفضيلُ مكتبة:** بها يجري الضابطُ على JVM بلا محاكٍ.
⚠️ و`URI` أصرمُ فيرمي على روابطَ يقبلها `Uri` ⇒ `false` ⇒ **`cancel`**: **فشلٌ مغلقٌ
مقصود** في دالّةٍ تقرّر تجاوزَ خطأِ شهادة.

**والضابطُ ثنائيُّ القطب** (`app/src/test/.../AppConfigSslTrustTest.kt`) ويجري في CI عبر
`testDebugUnitTest`: يُثبت **الحجب** (‏`evil.` و`lh3.` واللاحقةُ العارية · الانتحال ·
`attacker.workers.dev` · المدخلاتُ المعطوبة) **والسماحَ** (المسارات الخمسة المجمَّدة ·
`yemenschoolz.com` و`www.` · المسارُ الاحتياطيّ بمضيفه الكامل · `script.googleusercontent.com`).
🔴 **وأُثبت احمرارُه بطفرةٍ مقصودة** (إعادةُ اللاحقة عاريةً) ⇒ سقط `rejects_bare_user_content_suffix`
وحدَه ثمّ أُزيلت الطفرة — **فالأخضرُ شهادةُ سلامةٍ لا صمتُ مِجَسّ**.
⚠️ **وحدُّ التغطية يُقال:** التوجيهُ (`routeTargetFor`) **لا يُختبَر على JVM** لأنه يستعمل
`android.net.Uri` ⇒ عدمُ انكساره مسنودٌ **بالبناء** (‏`GOOGLE_HOSTS` لم تُمسّ) لا بالاختبار.
🔴 **وهذا الإصلاحُ لـ`vc36` — ولا يُدسّ في `vc35`** المختومة المؤرشَفة.

### مسارات Play والنشرُ المُدار — 🔴 مؤشّرٌ لا سرد

**المالكُ الكاملُ: [`_docs/مسارات-play-والنشر-المدار.md`](_docs/مسارات-play-والنشر-المدار.md)**
(نُقل بحرفه 2026-09-12 — إنذارُ `targetSdk` بسرده كاملاً، والنشرُ المُدار بضوابطه).
🔴 **ولا يُنسَخ منه سطرٌ إلى هنا.**

**والحاكمُ في خمسة أسطر:**

1. 🔴 **المطابقةُ تُقاس على اتّحاد المسارات الأربعة لا على الإنتاج وحدَه.** مسارُ اختبارٍ
   منسيٌّ يحمل حزمةً قديمة **يُبقي إنذارَ الحساب كلِّه** ولو كان الإنتاجُ مطابقاً — وقع فعلاً
   بحزمتَي أبريل. و`deploy-to-play.ps1 -DryRun` يطبع المسارات الأربعة في كتلةٍ واحدة.
2. 🟢 **`alpha` موقوفٌ مؤقتاً و`beta` «غير نشط»** — 🔴 **ولا يُعاد فتحُ `alpha`**: إعادتُه
   تعيد إنذارَ الحساب بحزمةِ أبريل نفسِها. وإسقاطُ حزمةٍ بائتةٍ **لا يحتاج بناءً ولا رقماً
   جديداً**: إيقافُ المسار — وهو ما يوصي به Play نصّاً — أو ترقيةُ المنشور إليه.
3. 🔴 **`completed` لا تشهد لشيء — بثلاثة شواهد:** لا للتوزيع · ولا لنشاط المسار · ولا لحالة
   الإيقاف. الحقلُ يصف **الطلب** وحدَه. ⇒ **لا يُبنى عليه حارسٌ أبداً.**
4. 🟢 **و`track-state.json` يخفّض الأحمرَ إلى ⚠️ مؤرَّخة** — **إقرارُ مالكٍ لا قياسٌ حيّ**،
   **مُثبَّتٌ برقم الحزمة تطابقاً لا احتواءً** ⇒ أوّلُ رفعةٍ جديدةٍ تُبطله ويعود الأحمرُ
   تلقائياً. 🔴 **والسببُ أن حارساً أحمرَ للأبد يُعلّم تجاهُلَ اللوحة** — وهو أخطرُ من غيابه.
5. 🔴 **والنشرُ المُدار مفعَّل ⇒ كلُّ تغييرٍ يمرّ بثلاثٍ: إرسالٌ ⇒ موافقةٌ ⇒ نشر.**
   **ولا أثرَ لأيّ تغييرٍ قبل الثالثة**، وقياسُ الأثر قبلها **يقيس لا شيء**.
   ⇒ **«فعلتُه» ليست «نفَذ».**

⚠️ **ومصدرُ «الحيّ» سطحان في Play Console وحدَهما** — «نظرة عامة على النشر» و**سجلُّ نشاط
إرسال الطلبات**. **ولا يُقاسان آلياً عندنا**، وهذا حدُّ الأدوات لا نقصُ اجتهاد.

---

## أرقامُ الإصدار والعدّادات — 🔴 مؤشّرٌ لا سرد

**المالكُ الكاملُ: [`_docs/ارقام-الاصدار-والعدادات.md`](_docs/ارقام-الاصدار-والعدادات.md)**
(نُقل بحرفه 2026-09-12 — تاريخُ المصالحة، والعدّاداتُ الأربعة، ودروسُ القياس).
🔴 **ولا يُنسَخ منه سطرٌ إلى هنا.**

**والحاكمُ في خمسة أسطر:**

1. 🔴 **لا يُجمَّد رقمٌ في هذا الملفّ** — لا `versionCode` ولا «الرقمُ التالي» ولا عددُ
   مستخدمين. كلُّ رقمٍ ثُبِّت هنا بطَل، **ورقمٌ مشتقٌّ يُجمَّد في وثيقةٍ يصير إملاءً بائتاً
   يُقرأ بثقةٍ ويُطبَّق، بينما غيابُه يدفع للقياس.**
2. 🔴 **الرقمُ التالي = آخرُ ما رُفع + 1، لا آخرُ ما نُشر + 1.** Play يحجز أيَّ رقمٍ **رُفع**
   ولو بقي مسوّدةً لم تُنشَر، ويرفض إعادةَ استعماله — وخطؤه يبدو غامضاً بلا هذا السطر.
   والمصدرُ الحيُّ أمرٌ واحدٌ **قراءتُه محضةٌ ولا تترك مسوّدة**:
   ```powershell
   pwsh -File .\deploy-to-play.ps1 -Track internal -DryRun
   ```
3. 🔴 **وهو مصدرٌ للرقم المحجوز لا لـ«الحيّ»** — يقرأ الـAPI، والـAPI يجيب عن **حالة الطلب**
   لا **حالة التوزيع**. ⇒ **لا يُبنى عليه حارسٌ لسطر «الحيّ» أبداً**؛ سيخضرّ على إصدارٍ لم
   يصل أحداً، والحارسُ الأخضرُ يُصدَّق بلا قراءة.
4. 🔴 **ولا يُقرأ رقمُ إصدارٍ من شجرةٍ لم تُحدَّث للتوّ**، **ولا من الكوميت الحاملِ للرقم** —
   فقد يكون كوميتَ **مصالحةٍ** كُتب بعد النشر بأسابيع. ⇒ **حين يوجد أثرٌ مبنيّ، تُقاس الحزمةُ
   لا الشجرة**؛ والشجرةُ تُسنَد بآخرِ كوميتٍ **قبل ساعة البناء** لا بالحامل للرقم.
5. ⚠️ **وعددُ المستخدمين لا يُنقَل من وثيقة** — Play يعرض عدّاداتٍ عدّةً مختلفةَ الفلاتر،
   وكلُّها صحيحةٌ معاً ولا يصحّح أحدُها الآخر. **يُقرأ من اللوحة لحظةَ الحاجة ويُسمّى
   عدّادُه معه دائماً.**

---

### الحزم لا تُلتزَم هنا — والقاعدة كانت ميّتة لسببين معاً

`app/release/app-release.aab` (‏4.2MB) و`.apk` (‏3.3MB) كانا **متتبَّعَين** في هذا المستودع
**العامّ**، رغم وجود سطرين في `.gitignore` يحاولان استبعادهما. فشلا لسببين مستقلَّين:

1. **مكتوبان بترميز UTF-16** (‏58 بايتاً صفرياً بين الأحرف) فلا يقرؤهما git إطلاقاً — أثبته
   `git check-ignore` لا القراءة البصرية، فالسطران يبدوان سليمين تماماً في المحرّر.
2. **و`.gitignore` لا أثر له على ملف متتبَّع** أساساً، فحتى بترميز سليم لما فعل شيئاً.

⇒ أُعيدت كتابته UTF-8، وفُكّ التتبّع بـ`git rm --cached` (**الملفات باقية على القرص** — مُتحقَّق)،
وأُضيف `*.aab`/`*.apk`/`app/release/` و`keystore.properties`/`*.jks`/`*.keystore`.
**التاريخ ما يزال يحوي النسخ القديمة** — إزالتها تحتاج إعادة كتابة تاريخ ودفعاً قسرياً، ولم يُطلب
(ولا سرّ فيها: الحزمة موزَّعة علناً على Play أصلاً، و`keystore.properties` لم يكن متتبَّعاً قطّ).

### أين تعيش الأشياء الآن

| الشيء | المكان |
|---|---|
| حزم الإصدار (‏نُقلت من سطح المكتب 2026-08-11، **وتنمو مع كلّ إصدار**) | `C:\Users\osama\Workspace\Releases\SchoolAppyemen\` — **و`README.md` هناك هو مالكُ الحقيقة**: أيُّ حزمةٍ حيّةٌ وأيُّها محظورة |
| مفتاح التوقيع | `C:\Users\osama\Workspace\Secure\schoolapp.jks` · alias `schoolapp` |
| ملف كلمتَي سرّ المفتاح | `C:\Users\osama\Workspace\Secure\` — **نُقل من سطح المكتب 2026-08-11** |

⚠️ ولا تخلط المفتاح بمفتاح «يمن سكولز» (`schoolzyemen-release.jks` · alias `schoolzyemen`) —
مفتاحان منفصلان تماماً، وتوقيع أحد التطبيقين بمفتاح الآخر يجعل Play يرفض التحديث بلا رجعة.

## 🧰 أتمتةُ هذا المستودع — بوّابتان وحارس (2026-09-07)

🔴 **وموضعُها كلِّها `.claude/` المتتبَّع** لا `~/.claude/skills/` المتجاهَل — فتُدفَع
وتُراجَع وتُستعاد. **وكلُّ فحصٍ فيها مشتقٌّ من عطلٍ وقع فعلاً**، لا من قائمةٍ عامّة.

| الأداة | متى | ماذا تقيس |
|---|---|---|
| **`/release-preflight`** | **قبل كلّ رفع** | ٧ فحوصٍ **من الحزمة المبنيّة نفسِها**: الختم · التوقيع · البصمة · مشروع FCM · `getFcmToken` · مسارات App Links · وهل الرقمُ محروق. تُخرج **GO / NO-GO / INCOMPLETE** |
| **`/play-audit`** | قبل أيّ قرارِ إصدار | حالةُ المسارات الأربعة · مسارٌ يحمل حزمةً **أقدمَ من الإنتاج** · والرقمُ الحرّ. ومنذ 2026-09-08 يقرأ **`track-state.json`** فيخفّض إقرارَ المالك إلى ⚠️ مؤرَّخة — **ويُبطله تغيُّرُ رقم الحزمة** |
| **`stale-bundle-guard`** | تلقائياً بعد كلّ تعديل | ينبّه إن صارت حزمةُ `outputs` **بائتةَ الختم** بعد تغيير رقم الإصدار |

🎯 **والمبدأ الجامع للثلاثة: الأثرُ المبنيُّ يشهد على نفسه، والشجرةُ مرجعُ مقارنةٍ لا
مصدرُ حقيقة.** ولا قيمةَ مجمَّدةً في أيٍّ منها — الهويّةُ والبصمةُ والمساراتُ تُقرأ وقتَ
التشغيل. وكلُّها **قراءةٌ محضة**: لا بناءَ ولا رفعَ ولا رقمَ يُحرق.

⚠️ **وحدُّها يُقال بدل الإيهام بحمايةٍ غير قائمة:** `GO` تعني «لا مانعَ بنيويّاً» **لا
«الحزمةُ تعمل»**. ويبقى ما لا تقيسه أداة: **تشغيلُ الحزمة على جهازٍ حقيقيّ** ·
`adb shell pm get-app-links` · وقراءةُ «حالة السياسة» و«نظرة عامة على النشر» في Console.
و`targetSdk` لحزمةٍ على Play **لا يُقرأ من الـAPI** أصلاً.

🔴 **والنشرُ المُدار مفعَّلٌ على هذا الحساب ⇒ لا أثرَ لأيّ تغييرٍ قبل «نشر التغييرات».**
الحكمُ والضوابطُ في §«مسارات Play والنشرُ المُدار» أعلاه، والسردُ كاملاً في
[`_docs/مسارات-play-والنشر-المدار.md`](_docs/مسارات-play-والنشر-المدار.md).

---

## 🏗️ CI — بناءُ debug على كل PR (أُضيف 2026-08-29)

قبل هذا التاريخ كان المستودع يحمل `claude.yml` **وحده**: لا شيءَ يتحقّق أن الكود
يُبنى أصلاً — في تطبيقٍ **منشور**. `build.yml` يشغّل `./gradlew assembleDebug`
على كل PR وعلى `push` إلى `main`.

🟢 **و`assembleDebug` عمداً — والسبب أمنيّ لا أدائيّ:**
- لا يحتاج `keystore` ولا أي سرّ توقيع ⇒ **صفرُ مساسٍ بـ`Workspace\Secure`**،
  ولا يلزم أي سرٍّ جديد في GitHub Actions.
- و`app/build.gradle.kts` يحرس ذلك بنفسه: التوقيع مشروطٌ بـ`if (keystoreFile.exists())`
  ⇒ غيابُ `keystore.properties` على العدّاء **مسارٌ مدعومٌ لا عطل**.

🔴 **ولا يُضاف `assembleRelease` بلا قرارٍ صريح:** يستدعي حقن أسرار التوقيع في CI،
وهو قرارٌ أمنيّ منفصل لا ترقيةٌ تقنية.

### 🔴 وأوّلُ تشغيلٍ كشف عطلاً كامناً منذ إنشاء المستودع

`./gradlew: Permission denied` (خروج 126) — **بتُّ التنفيذ غيرُ مضبوطٍ في شجرة git**
(‏`100644` بدل `100755`). وليس خصوصيةَ عدّاء: **أيُّ استنساخٍ على لينكس أو ماك يفشل
بالطريقة نفسها**، وقد ظلّ كذلك بلا أن يظهر لأن لا CI كان يبني.

🟢 **وعولج بالبتّ نفسه لا بـ`chmod +x` في الوركفلو** — ذاك يُخضِّر CI ويترك العطل
قائماً لكل بشريّ يستنسخ. ⇒ خضرةُ البناء اليوم **دليلٌ أن البتّ صحيح**، لا التفافٌ عليه.

### 🤖 `claude.yml` — الصلاحياتُ `write` ولا تُعاد إلى `read`

عطبُ `read` موثَّقٌ رسمياً (`claude-code-action#1562`): الإجراء يعمل ثمّ **لا ينشر
تعليقاً ولا مراجعة، بلا أي رسالة خطأ**.

📖 **والمالك الواحد لحقيقة CI عبر المستودعات: `~/docs/ci-github.md`.**
