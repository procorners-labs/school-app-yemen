# إعدادات Google Cloud Platform – منظومة مدارس الإبداع والتميز الدولية

**رقم المشروع:** `108410742569`
**اسم المشروع:** `school-494822`
**المالك:** `procorners.shop@gmail.com`

> ملاحظة تاريخية: تطبيق الأندرويد كان يستخدم بالخطأ `app/google-services.json` لمشروع Firebase
> مختلف تماماً (`mkrh-181cd`) — سُرِّب مفتاح API الخاص به علناً عبر GitHub Secret Scanning
> (commit `14f5409`). المالك حذف مشروع `mkrh-181cd` بنفسه، وصُحِّح الملف إلى `school-494822`
> الصحيح (PR #6، مدموج). ✅ تنبيه GitHub الأمني #1 أُغلق كـ"Revoked" بموافقة صريحة من المالك
> (2026-07-09).

## المشاريع/الملفات المرتبطة بـ GCP (عبر Apps Script)

| التطبيق | المعرف | الحالة |
|---------|--------|--------|
| الموقع الرسمي | `1J7DY-Z2PZU5y5HH-LR3vhuEhPAkjWz22vMu1rYLcse0` | ✅ مرتبط |
| منصة الطالب | `1BPtHUMB8kdi2exbfPVaKoSOcjGGZrnbJANXPHnWTD_A` | ✅ مرتبط |
| منصة المعلم | `1G6sLNJZqZ2pazx22nNS6X6GIYfAE-rT2IjcrF9NSheM` | ✅ مرتبط |
| CMS | `1J7DY-Z2PZU5y5HH-LR3vhuEhPAkjWz22vMu1rYLcse0` | ✅ مرتبط |
| أداة الحصص | `14VflEuGRCXIOz22_cYp2HmUTZYYsMJ1fNtvDMxbYSZA` | ✅ مرتبط |

## الخدمات المفعلة

- ✅ Drive API
- ✅ Sheets API (عبر Apps Script)
- ✅ Script API

## تطبيقات Firebase المسجَّلة تحت `school-494822`

| المنصّة | App ID | اسم الحزمة | تاريخ التسجيل |
|---|---|---|---|
| Android | `1:108410742569:android:cd60f6317dbb3f4714b938` | `com.proconrers.schoolappyemen` | 2026-07-09 (PR #6، يستبدل تسجيل `mkrh-181cd` الخاطئ) |

✅ بصمة SHA-256 (`11:E9:B0:2B:1F:26:06:54:04:F8:64:46:51:F8:FA:84:EC:52:DF:3D:0D:11:16:9B:E3:E9:
E3:40:B7:50:FA:39`) أُضيفت 2026-07-09 عبر `firebase_create_android_sha` — المالك زوّد البصمة عبر
تشغيل `check-signing.ps1` محلياً (لا تظهر كلمة مرور الـkeystore أبداً خارج الجهاز المحلي).

## حسابات الخدمة (Service Accounts)

> 🟢 **مقيسٌ بـ`gcloud` 2026-09-06 — والجدولُ أدناه واقعُ المشروع لا نيّتُنا.**
> كان هنا **حسابان**؛ والمشروعُ يحمل **خمسة**. الثلاثةُ الزائدة لم تكن مخفيّة، بل
> **لم تُسأل عنها وثيقةٌ قطّ** — وأحدُها مُوثَّقٌ على جهاز المالك.

| الحساب (`@school-494822.iam.gserviceaccount.com`) | أدوارُ IAM على المشروع (مقيسة) | مفاتيحُ مستخدِم | الاستخدام · أين يُستدعى |
|---|---|---|---|
| `play-publisher` | **لا شيء** | **1** | نشرُ إصدارات Android (‏Android Publisher API v3) · `deploy-to-play.ps1` محلياً عبر `service-account-key.json` (غيرُ مُتعقَّبٍ في git) |
| `teacher-platform-drive` | `androidmanagement.user` · `bigquery.connectedSheetsServiceAgent` · `serviceusage.serviceUsageAdmin` | 0 | مرفقاتُ أخبار المعلّمين وCMS · Apps Script عبر `DriveApp` |
| `school-sync` | **لا شيء** | **1** | ⚠️ **غيرُ مؤكَّد** — الأرجحُ مزامنةُ Access→Sheets في `SchoolApp-gas`. **يُسأل مالكُ ذلك المستودع** |
| `school-reader-sa` | 🔴 **`roles/editor`** · `firebase.managementServiceAgent` · `iam.serviceAccountViewer` | **1** | ⚠️ **غيرُ مؤكَّد** — **ومُوثَّقٌ في `gcloud` على جهاز المالك** (يظهر في `gcloud auth list`) |
| `firebase-adminsdk-fbsvc` | `firebase.sdkAdminServiceAgent` · `iam.serviceAccountTokenCreator` | 0 | يُنشئه Firebase تلقائياً · لا يُحذف |

🔴 **وأخطرُ سطرٍ في الجدول `school-reader-sa`: اسمُه يقول «قارئ» ودورُه `roles/editor`.**
وهو دورٌ واسعٌ يشمل الكتابةَ والحذفَ في مواردِ المشروع، **وله مفتاحٌ مُدارٌ بالمستخدم**،
**وهو مُوثَّقٌ على الجهاز**. ⇒ **الاسمُ يخدع من يقرؤه، والدورُ هو الحاكم.**
🔴 **ولا يُخفَّض ولا يُعطَّل قبل معرفة قارئه** — قاعدةُ «اتبع القارئَ لا المُعلِن»:
حسابٌ بلا استعمالٍ ظاهرٍ قد يكون قلبَ أتمتةٍ لا تُرى من هنا. البندُ في
`~/.claude/health/pending.json`.

🟢 **و«لا شيء» في عمود الأدوار ليست خللاً بالضرورة — وهي تُثبت قاعدةً مكتوبة:**
`play-publisher` **بلا أيّ دورِ IAM** لأن صلاحيةَ النشر تعيش في **Play Console** لا في
GCP. ⇒ **لا يُبحث عنها هنا، ولا يُستنتج من غيابها شيء.** والأرجحُ أن `school-sync`
مثلُه: وصولُه على مستوى **المورد** (مشاركةُ Sheets/Drive) لا المشروع.

⚠️ **ودعوى قديمة لم يُسندها القياس:** كان مكتوباً أن `teacher-platform-drive` له
**`roles/drive.file`** — وهو **ليس** ضمن أدواره على المشروع. **ولا يُقرأ ذلك نفياً
لقدرته على Drive**: وصولُ Drive يُمنح غالباً **بمشاركة المجلد** لا بدورِ مشروع
(والمجلدان أدناه). ⇒ **الدعوى تُصحَّح إلى «غيرُ مقيسة»، لا إلى «خاطئة».**

**ولا تخلط بين مسارين:** `teacher-platform-drive` و`school-sync` يخصّان **محتوى
المنصّة**؛ و`play-publisher` يخصّ **نشرَ تطبيق الأندرويد حصراً** — منفصلٌ تماماً عن
أيّ مزامنة GAS.

### حدودُ هذا التدقيق — تُقرأ قبل البناء عليه

1. **قِيست أدوارُ IAM على مستوى المشروع وحدها.** المنحُ على مستوى **المورد**
   (مشاركةُ مجلدٍ في Drive · ورقةٍ في Sheets) **لا يظهر هنا إطلاقاً**.
2. **صلاحياتُ Play Console خارجَ GCP** ⇒ لم تُقَس، ولا يُدَّعى قياسُها.
3. **عُدَّت المفاتيحُ ولم تُقرأ** — معرّفاتٌ فقط، وصفرُ مساسٍ بأيّ مادّةٍ سرّية.
   **وعددُ المفاتيح لا يقول أين تعيش** ولا هل هي متداولة.
4. 🔴 **ولم يُثبت حذفُ `mkrh-181cd`** (الملاحظةُ التاريخية أعلاه): الـAPI ردّ
   «لا صلاحية… **أو قد لا يكون موجوداً**» — **وهو يخلط الغيابَ بانعدام الصلاحية**،
   فلا يُقرأ إثباتاً في أيّ اتجاه.

**وإعادةُ القياس** (‏قراءةٌ محضة · من PowerShell، أو من bash بعد `CLOUDSDK_PYTHON`):
```
gcloud iam service-accounts list --project=school-494822
```

## المجلدات المرتبطة (Drive)

- مرفقات أخبار المعلمين: `17KtwtRIsk0I96fl5UakRKhfoSk0ZjUQF`
- مرفقات CMS: `13A82NOATnZTuk5EtVfo2hplZih5NFSnT`

## Google Analytics (GA4) — موجود على مستوى الحساب، غير مُفعَّل في أي كود حيّ (عمداً)

يوجد حساب Google Analytics باسم **"Procorners Education"** (`accounts/397644779`) تحت نفس مالك
GCP، بخاصيتين:

| الخاصية | تاريخ الإنشاء | الحالة |
|---|---|---|
| "مدارس الإبداع والتميز" (`properties/541178372`) | 2026-06-10 | كانت مربوطة بوسم `gtag` في الموقع، **أُزيل نهائياً بقرار صريح من المالك (2026-07-04) لأنه سبّب مشاكل** — لا تُقترح إعادة ربطها |
| "school-494822" (`properties/544239746`) | 2026-07-05 (آخر تعديل 2026-07-08) | خاصية غير مرتبطة بأي كود حيّ حالياً — أُنشئت أثناء عمل تصحيح Firebase في جلسة سابقة، مُبقاة كما هي دون حذف بقرار المالك |

**⚠️ لأي جلسة/وكيل مستقبلي:** وجود هاتين الخاصيتين على مستوى حساب Google Analytics **لا يعني** أن
تتبّع الزوار مفعَّل — تحقّقتُ أنه لا يوجد أي `gtag`/`measurementId`/معرّف `G-` في كود الموقع
(`school-app-yemen-web`) أو الواجهة المُولَّدة. هذا وضع **مقصود**؛ لا تقترح ربط أي منهما بالكود من
تلقاء نفسك دون طلب صريح من المالك.

## 🔵 شاشة موافقة OAuth — الملفُّ **مُغلَقٌ بقرارٍ معتمَد**، واللافتةُ ليست عُطلاً

> **يُقرأ حين تظهر لك في الكونسول:** «‏Your app requires verification» · Publishing status
> `In production` · User type `External` · **2 / 100 user cap**.
> **الجوابُ: لا شيءَ معطَّل، ولا إجراءَ مطلوب.** هذه لافتةٌ باقيةٌ لمرحلةٍ **حُكم عليها بأنها
> اختيارية فلا تُقدَّم** — بقرارٍ مؤرَّخ 2026-08-09، لا باستنتاجٍ جديد.

🔴 **والمالكُ الواحد لهذا الملفّ خارجَ هذا المستودع — لا يُعاد سردُه هنا فيتباعد:**
[`SchoolApp-gas/_docs/2026-08-09-نجاح-oauth-branding-والمرحلة-التالية.md`](../../SchoolApp-gas/_docs/2026-08-09-نجاح-oauth-branding-والمرحلة-التالية.md)
— فيه الحالةُ النهائية وخطُّ الأساس المرجعيّ للكونسول والقرارُ وشرطا إعادة فتحه.
وسلسلتُه: `2026-07-24-توثيق-branding…` · `2026-07-25-تعارض-نطاق-مشترك…` ·
`2026-07-29-تشخيص-رفض-oauth-branding-الخامس…` · `2026-08-05-اسقاط-نطاق-gmail-المقيد.md`.

**الخلاصةُ في ثلاثة أسطر** (والتفصيلُ في المصدر أعلاه):
- ✅ **Branding مُتحقَّقٌ ومعروضٌ للمستخدمين** منذ 2026-08-09 بعد ستّ رفضات — حسمه توحيدُ
  الاسم على **`Yemen Schoolz`** بترتيبه الموافق لسلسلة النطاق `yemenschoolz.com`.
- ✅ **`In production`** ⇒ **انتهاءُ رمز التحديث كل ٧ أيام سقط جذرياً** (كان خاصّيةَ حالة
  `Testing` حصراً)، وهو السببُ الجذريّ الموحَّد لكلّ حوادث «التفويض العالق» السابقة.
- 🎯 **Data access verification: اختياريةٌ ⇒ لا تُقدَّم.** ثمنُها مراجعةٌ بشرية + فيديو +
  **تقييم CASA بطرف ثالث** (يفرضه `auth/drive` المقيَّد في أربعة تطبيقات) ⇒ أسابيع؛ وما
  تشتريه: إزالةُ شاشةٍ **يراها المالكُ وحده مرّة**، ورفعُ سقفٍ عدّادُه **٢** ولا مسارَ يرفعه.

**وشرطا إعادة الفتح — مُعلَنان، وأيٌّ منهما وحده يكفي:**
1. أن يصير الدخولُ **بحساب Google** لمستخدمين حقيقيين ⇒ السقفُ يصير حقيقياً.
2. أن يُزعج تحذيرُ «تطبيق غير موثَّق» **مستخدماً غير المالك**.

🔴 **وإن أُعيد فتحه يوماً فالترتيبُ إلزاميّ:** تدقيقُ `drive` ← `drive.file` **أوّلاً** —
نجاحُه يُسقط CASA كلّياً. و🔴 تعديلُ أيّ `appsscript.json` **بيد المالك حصراً** (يحرسه هوك
`protect-deploy-ids.js`).

🔴 **وفخُّ التفويض الصامت — يُقرأ قبل إضافة أيّ نطاق، لا بعدها:**
**Apps Script لا يُعيد عرضَ شاشة الموافقة** ما دام يعتبر تفويضَ السكربت الحاليَّ سارياً —
**حتى بعد إضافة نطاقاتٍ للبيان ونشرِها بنجاح**. ⇒ النطاقُ الجديد يبقى **غيرَ ممنوحٍ بصمت**،
ولا شاشةَ تظهر ولا خطأَ يُنبّه. قِيس 2026-07-29: `master-admin?action=health` ⇒ **`passed: 8/11`**
والفاشلةُ `calendar.readonly` · `script.projects` · `script.deployments`، **بينما `clasp pull`
يُثبت أن البيانَ المنشور يحمل الأحدَ عشرَ فعلاً** — أي أن الإعلانَ تمَّ والمنحَ لم يتمّ.
**والإجراءُ الحتميّ بهذا الترتيب:** ① إبطالُ المنحة من `myaccount.google.com/permissions` ⇒
② تشغيلُ `_authorizeAllScopes` **من `master-admin` حصراً** — تشغيلُها من `teacher` يمنح
خمسةَ نطاقاتٍ فقط. (المصدر: `SchoolApp-gas/_docs/2026-07-29-تفويض-موحد-من-دالة-واحدة.md`.)

### 🔴 ولماذا لا يمسّ سقفُ «2 / 100» مستخدميك — لأنه أشهرُ ما يُساء قراءته

النشراتُ الثماني كلُّها `"executeAs": "USER_DEPLOYING"` + `"access": "ANYONE_ANONYMOUS"`
⇒ النصوصُ تجري **بهوية المالك وحده**، والمعلّمُ والطالبُ يدخلان بكلمات مرور المنصّة من الشيت
(‏`withAuth`) **لا بحساب Google** ⇒ **لا يمنحان إذناً ولا يستهلكان مقعداً**.
⇒ **«2 / 100» حسابا Google فوّضا فعلاً — لا مستخدمان من المنصّة.**
⚠️ **ولا يُنسَب العددُ إلى الاستعمال:** المنصّةُ تخدم ٦٦٣ تثبيتاً والعدّادُ ٢ بعد أكثر من عام.

### 🟢 وحصّةُ هذا المستودع من الملفّ: صفر — مقيسةٌ لا مفترَضة

تطبيقُ الأندرويد **لا يمسّ شاشةَ الموافقة إطلاقاً**. `grep` على ١٦ ملفَّ Kotlin و
`app/build.gradle.kts` ⇒ **صفرُ `GoogleSignIn` · صفرُ `AccountManager` · صفرُ
`play-services-auth` · صفرُ نطاق OAuth**.
🟢 **بضابطٍ موجب في نفس الأمر وعلى نفس الملفّات:** `firebase` يُطابَق في خمسة مواضع
⇒ الفحصُ يقرأ فعلاً، و«صفرُ المطابقة» نتيجةٌ لا عطبُ أداة.

⚠️ **وفخٌّ يقلب الجواب:** `app/google-services.json` يحوي `oauth_client` واحداً من نوع
`client_type: 3` (عميلُ ويبٍ يولّده Firebase تلقائياً) — **ولا سطرَ واحداً يستعمله**.
⇒ **الحضورُ ليس اعتماداً**، وهو نفسُ فخّ `trustedSslDomains` المسجَّل في `CLAUDE.md`.

### ⚠️ حدودُ ما في هذا القسم

1. **حالةُ اللوحة منقولةٌ عن لقطةِ المالك** (2026-09-02) لا مقروءةً من API ⇒ تتقادم بأيّ
   تغييرٍ في الكونسول. وهي **مطابقةٌ لما سجّله مصدرُ 2026-08-09** حرفياً (‏`In production` ·
   `External` · ٢/١٠٠) ⇒ **صفرُ انجرافٍ في ثلاثة أسابيع**.
2. **جردُ `executeAs` والنطاقات قِيس من الشجرة لا من النشرة المخدومة**، و`clasp deploy` يمكن
   أن يتجاوز المانيفست لو نُشر يدوياً من المحرّر. يحسمه `served-manifest-guard` في `SchoolApp-gas`.
3. **بندان يبقيان مفتوحَين ولا يحجبان شيئاً:** تدقيقُ `drive` ← `drive.file` (تحسينٌ أمنيّ
   مستقلّ)، وقراءةُ تحذيرات Project Checkup الثلاثة (إرشادية). كلاهما في المصدر §٦.

**مصادرُ القياس:** جردُ `executeAs`/`access` للثمانية — جلستا `split-student-gas-deployment`
و`root-scope-coordination-fix` · جردُ النطاقات — `tighten-device-permissions-gas` ·
فحصُ الأندرويد و`google-services.json` — هذه الجلسة.

---

## ملاحظات

- تم حل مشكلة رفع الصور عبر `DriveApp` باستخدام صلاحية المجلد العام.
- تاريخ آخر تحديث: **2026-09-02** (إضافة قسم شاشة موافقة OAuth) — وقبله 2026-07-09.
