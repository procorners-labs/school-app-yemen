# قياسُ حزم `vc31` · `vc34` · `vc35` — عقودُ المضيفات والـUA وفئتا ثغرة الشهادة

> **التاريخ:** 2026-09-15 · **الجلسة:** `schoolappyemen-46` · **قراءةٌ محضة**: صفرُ بناءٍ
> وصفرُ رفعٍ وصفرُ توقيع.
>
> **سببُ القياس:** ثلاثُ جلساتٍ (`schoolapp-cd` · `fix-school-routing-ci` · `yemenschoolz-d7`)
> كانت تحذف مساراتٍ لمشاريعَ متقاعدة في مستودعاتٍ أخرى، وبنت قرارَها على **شجرةِ عملِ** هذا
> المستودع. ⇒ **الشجرةُ تصف ما نبني، والحزمةُ تصف ما يعمل عند المستخدم.**

---

## ١. هويّةُ ما قِيس — البصمةُ أوّلاً

الحزمُ من `Workspace\Releases\SchoolAppyemen\`، وبصماتُها **طابقت `README.md` هناك حرفياً**
⇒ الحزمُ هي ما تدّعيه:

| الحزمة | `sha256` (أوّل ١٢) |
|---|---|
| `SchoolApp-v2.8-vc31-2026-07-17.aab` | `0d8d71ece648` |
| `SchoolApp-v3.1-vc34-2026-09-05.aab` | `90b79b8ed42b` |
| `SchoolApp-v3.2-vc35-2026-09-07.aab` | `a1d86753e099` |

🔴 **ولماذا تُقدَّم البصمةُ على الاسم والحجم:** حزمتان في مجلد البناء فارقُهما **بايتان**
(‏٣٬٣٣٤٬٧٢٨ مقابل ٧٣٠) إحداهما تكذب في مانيفستها. **الحجمُ لا يفرز، والبصمةُ تفرز.**

---

## ٢. المضيفُ تبدّل بإصدارٍ لا بإعداد

```
grep -aoE 'https://[A-Za-z0-9./?=_%:&-]+' base/dex/classes.dex
```

| | `vc31` | `vc34` (منشور) | `vc35` |
|---|---|---|---|
| `CANONICAL_ORIGIN` | `school.procorners.com` | **`yemenschoolz.com`** | **`yemenschoolz.com`** |
| `/home/index.html` | ✅ | ✅ | ✅ |
| `/teacher/index.html` | ✅ | ✅ | ✅ |
| `/student/index.html` | ✅ | ✅ | ✅ |
| `/gas/teacher` | ✅ | ✅ | ✅ |
| `/cms/index.html` | ✅ | ❌ | ❌ |
| `/home/newsarticle.html` | ❌ | ✅ | ✅ |
| `/schedule/**` | **❌** | **❌** | **❌** |
| `home-all-school` | ❌ | ❌ | ❌ |

**عددُ ورودِ المضيفَين** (‏dex · مانيفست):

| | `vc31` | `vc34` | `vc35` |
|---|---|---|---|
| `yemenschoolz.com` | ٠ · ٠ | **٧ · ٢** | **٧ · ٢** |
| `school.procorners.com` | **٥** · ٠ | **١** · ٠ | **١** · ٠ |

⚠️ **والعددُ وحدَه يُضلّل ما لم يُقرأ سياقُه:** الواحدةُ في `vc34` **ليست وجهةَ طلب** بل
مُدخَلُ `PLATFORM_HOSTS`. ونظيرُها `procorners.com` العاري ⇒ `TRUSTED_SSL_HOSTS`.
**الحضورُ ليس اعتماداً.**

### العقدُ المستخلَص — اتحادُ الإصدارين الحيّين

- 🔴 **`/student/index.html` ⇒ ٢٠٠ على `school.procorners.com` و`yemenschoolz.com` معاً.**
- 🔴 **`/cms/index.html` ⇒ ٢٠٠ على `school.procorners.com` وحدَه** (‏`vc31`). **بندٌ كان
  خارج جردِ كلّ الجلسات** ⇒ كان سيسقط صامتاً في حذفٍ بالجملة.
- 🟢 **`/schedule` ⇒ صفرُ عقدٍ من جهتنا.** `SCHEDULE_URL` بلا مستهلكٍ في أيّ نشاط ⇒ يُسقطه
  R8 من كلّ بناء. (‏`vc31` وحدَه يحمل تعبيراً نمطياً `/(home|student|teacher|cms|schedule)/`
  يقرّر «ابقَ في الـWebView» لرابطٍ **تعرضه الصفحة**، لا طلباً يُصدره التطبيق.)
  ⚠️ **وإبقاؤه قرارُ حيطةٍ عند مالك الوركر** — «صفرُ مطابقةٍ في الحزمة» تُثبت أن **التطبيقَ**
  لا يطلبه، **ولا تُثبت أن لا أحدَ يطلبه**: المستهلكُ قد يكون إنساناً.
- 🟢 **`home-all-school` ⇒ صفرُ اعتماد.** مرجعُه الوحيد تعليقٌ في `AppConfig.kt:207`.
  ⚠️ **ولا يُحذف المسارُ لأجلنا** — له ثلاثةُ مستهلكين في مستودع الوركر (‏`school-app-proxy.js`
  موضعان · `test-routes.js` · `_build/gen-sitemap.js`)، **والتعليقُ عندنا ليس بائتاً بعد.**

---

## ٣. توجيهُ ٣٠١ — شرطان مختلفان لا متدرّجان

قُرئ من كائن git عند **`c51e3ae`** (كودُ ما قبل نشر `vc31`) ومن الشجرة الحالية:

| الإصدار | الآليّة |
|---|---|
| `vc31` | `url.contains("/student/")` — **يتجاهل المضيفَ، ويصرم في المسار** |
| `vc34/35` | `isPlatformHost(host)` **أوّلاً** ⇒ `EXTERNAL`، ثمّ المسار — **العكسُ تماماً** |

| التحويلُ ٣٠١ | `vc31` | `vc34/35` |
|---|---|---|
| يُبقي المقطعَ على مضيفِ منصّة | 🟢 يتبعه | 🟢 يتبعه |
| **يغيّر المقطع** (‏`/student/index.html` ⇒ `/portal`) | 🔴 **متصفّح** | 🟢 يتبعه |
| يخرج إلى مضيفٍ خارج القائمة | 🟢 يتبعه (!) | 🔴 **متصفّح** |

🎯 **⇒ الحاكمُ اتحادُ القيدين ما دام `vc31` حيّاً، لا «أحدُهما أشدّ».**
🔴 **وصياغةٌ مطلقة تخطئ في الاتجاهين** — تمنع الآمنَ وتُجيز القاتل.

> ⚠️ **تصحيحٌ لوثيقةٍ خارج هذا المستودع:** `~/CLAUDE.md` §رابعاً يقول إن التطبيقين
> «يوجّهان بمقطع المسار **ويتجاهلان المضيف**». **صحيحةٌ حرفياً لـ`vc31`، وبطَلت بـ`vc34`.**
> **العلّةُ صيغةُ الحاضر لا المضمون** — ومن يقيسها على `vc31` يجدها صادقةً فتكتسب ثقةً
> تُنفَق على إصدارٍ آخر. **التصحيحُ هناك بيد المالك من جلسةٍ جذرُها `C:\Users\osama`.**

---

## ٤. ثغرةُ تجاوز الشهادة — فئتان، والثانيةُ على كلّ الأجهزة

| الفئة | `vc31` | `vc34` (حيّ) | `vc35` | الإصلاح |
|---|---|---|---|---|
| ① `url.contains(domain)` على الرابط كلِّه | 🔴 | 🟢 | 🟢 | `46cfa3f` · **2026-08-12** — قبل بناء `vc34` |
| ② `googleusercontent.com` **عارياً** | 🔴 | 🔴 | 🔴 | `f6f5160` · **2026-09-09 7:04م** — **بعد الحزمتين** |

🔴 **⇒ الفئةُ ② لا تسقط بانقراض `vc31` بل بوصول `vc36`**، و**`vc35` المختومةُ تحملها**.
⚠️ **وحدُّ الخطر:** يلزمه رابطٌ معادٍ **داخل الـWebView** **و**شهادةٌ خاطئة — ليس استغلالاً
بنقرة، وليس مستبعَداً.
📌 مسجَّلٌ في `~/.claude/health/pending.json` ⇒ `vc31-ssl-trust-by-substring-on-live-devices`.

---

## ٥. حدودُ هذا القياس — تُقال ولا تُطوى

1. **«`vc34` منشور» ليس قياسَ هذه الجلسة** — بل **إقرارُ مالكٍ من Play Console** في
   2026-09-06 (‏طلبُ الإرسال #20). والـAPI لا يشهد: `Edits.tracks` ردّ `completed`
   **قبل النشر وبعده حرفياً** بقياسين ⇒ يجيب عن **حالة الطلب** لا **حالة التوزيع**.
2. **نسبةُ من هم على كلّ إصدار غيرُ متاحة** ولم تُقدَّر. و«٦٦١ تثبيتاً» عدّادٌ مختلفُ الفلتر
   **لا نسبةُ تبنٍّ**. تُقرأ من Play Console ← الإحصاءات ← الإصدارات.
3. **السلوكُ مستنتَجٌ من الكود، والحضورُ مقيسٌ من `dex`** — **لم تُشغَّل حزمةٌ على جهاز.**
4. **فروعُ `vc31` من كائن git لا من `dex`** — سندُها المستقلُّ أن التعبيرَ النمطيَّ حاضرٌ في
   dex `vc31` وغائبٌ عن `vc34/35`؛ **سطحٌ يسند ولا يُثبت باقي الفروع.**
