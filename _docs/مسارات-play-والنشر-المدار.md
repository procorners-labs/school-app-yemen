# مسارات Play الأربعة · إنذارُ `targetSdk` · والنشرُ المُدار

> 🔴 **هذا الملفُّ هو المالكُ الكاملُ لهذا السرد** — نُقل بحرفه من `CLAUDE.md` في 2026-09-12
> (‏الأسطر 607–701 و835–898 هناك) بعقدِ «صفر فقد»: **ولا سطرَ منه يُنسَخ إلى `CLAUDE.md`**.
>
> **والحكمُ التنفيذيُّ وحدَه بقي هناك** في §«مسارات Play» — والتفصيلُ والقياساتُ والتواريخ هنا.
> والقسمان مفصولان بفاصلٍ أدناه: أوّلُهما إنذارُ `targetSdk`، وثانيهما النشرُ المُدار.

---

### 🟢🟢 قيدٌ ثالثٌ — **زال الإنذارُ 2026-09-07: «تمت معالجة الانتهاكات» · 1:07م**

> **حُسم 2026-09-08 بقياسين من Console.** والقاعدةُ أدناه **تبقى كاملةً** — فهي التي كشفت
> الجاني، وأوّلُ رفعةٍ إلى مسارِ اختبارٍ تُعيد الفئةَ من جديد.

**والمقيسُ حسم شقّاً كان مكتوباً هنا خطأً — يُصحَّح لا يُحذف:**

| الشقّ | ما كان مكتوباً | 🟢 **المقيس 2026-09-08** |
|---|---|---|
| `alpha` | «يُوقَف» | **موقوفٌ فعلاً** — مُدرَجٌ تحت «المسارات المتوقِّفة مؤقتًا: الاختبار المغلق - Alpha» |
| `beta` | 🔴 «يحتاج نفسَ الخطوات الثلاث» | 🟢 **لم يحتج شيئاً قطّ** — قناتُه **«غير نشط»** و«هذا الإصدار حلَّ محلّه إصدار آخر». ⇒ **الدعوى تسقط بالقياس لا بالتنفيذ** |

⇒ **`alpha` وحدَه كان الجاني**، وإيقافُه وحدَه كفى — واللوحةُ كانت تقول ذلك عن `beta` من
البداية، **والـAPI هو الذي خالفها**.

⚠️ **وتباينٌ زمنيٌّ يُكتب حدّاً ولا يُفسَّر بالتخمين:** «وقتُ حلّ المشكلة» **1:07م** يسبق
نشرَ إيقاف `alpha` المسجَّل عندنا (‏طلب #21 · **4:55**) بـ~٣س٤٨د. ⇒ **السببيّةُ مرجَّحةٌ
لا مُثبَتة**. 🔴 **ولا يُدَّعى «إيقافُنا هو ما أصلحه» إثباتاً.**

🟢🟢 **وانتهى التباينُ في اليوم نفسِه — بسجلّ أنشطة Play:** سجلُّه بصيغة **٢٤ ساعة**،
وفيه إيقافُ `alpha`: «تم إرسال التغييرات للمراجعة» `04:29` ⇒ «وضع النشر» `04:55`
⇒ أي **٤:٥٥ صباحاً**، و«حُلّت المشكلة 1:07م» = `13:07` ⇒ **بعده بثماني ساعات، بلا أيّ
تناقض**. ⇒ 🟢 **السببيّةُ صارت متّسقةً زمنياً**، و**مرشّحُ المنطقة الزمنية سقط**.
(‏ويؤكّده الصفُّ نفسُه لـ`vc34`: `16:51` إرسال · `18:21` نشر — مطابقٌ لتصحيح «6:21م».)
⚠️ **والدرسُ يبقى وإن زال موجبُه: وقتٌ يُسجَّل بلا `ص`/`م` يخترع تناقضاً حيث لا تناقض**
— كلّف هذا فرضيّتين وساعات. **كلُّ وقتٍ يُكتب هنا يحمل لاحقتَه.**
⚠️ **والدرسُ أعمُّ من الحادثة: وقتٌ يُسجَّل بلا `ص`/`م` يخترع تناقضاً حيث لا تناقض** —
وقد كلّف هذا فرضيّتين. **كلُّ وقتٍ يُكتب هنا يحمل لاحقتَه.**

🟢 **والمهلةُ الممدَّدة (1 نوفمبر 2026) سقط موجبُها**، والبندُ
`play-target-api-warning-from-stale-test-tracks` **مغلق**.
🔴 **ولا يُعاد فتحُ `alpha`** — إعادتُه تعيد إنذارَ الحساب بحزمةِ أبريل نفسِها.

**والسردُ الأصليُّ يبقى لأنه القاعدةُ لا الحادثة:**

بقي إنذارُ Play «يجب أن يستهدف التطبيق API 36» قائماً **بعد** نشر `vc34/3.1` بـ`targetSdk 36`
على الإنتاج، وبدا لغزاً يُلقى على النشر المُدار أو على سقوط الرفع الآليّ. **ولم يكن أيّاً منهما.**

**الصفحةُ نفسُها تسمّي الجناة** في قسم «حِزم التطبيقات التي خضعت للمراجعة» — وهو القسمُ الذي
يُقرأ **أوّلاً** لا آخراً:

| الحزمة | المسار | `targetSdk` | التاريخ |
|---|---|---|---|
| `5 (1.2)` | الاختبار المفتوح (`beta`) | **35** | 15 أبريل 2026 |
| `4 (1.2)` | الاختبار المغلق - Alpha (`alpha`) | **35** | 13 أبريل 2026 |

وسطرُ «أعلى مستوى مستهدَف **غير متوافق** هو API 35» يؤكّده من الجهة المقابلة: `vc34`
ليس في القائمة أصلاً. ⇒ **إصداران من `v1.2` عمرُهما خمسةُ أشهر، نشطان على مسارَي اختبارٍ
منسيَّين، يُبقيان إنذارَ الحساب كلِّه** بينما الإنتاج مطابقٌ تماماً.

🔴 **والفئةُ مسجَّلةٌ سلفاً في `~/CLAUDE.md` §Measurement Discipline: «الأداةُ أجابت عن سؤالٍ
غير الذي سُئلت».** قرأنا الإنتاجَ وحكمنا على **الحساب**. والقاعدةُ تُعمَّم: أيُّ متطلَّبِ
مطابقةٍ عند Play (‏`targetSdk` · الأذونات · سياساتُ المحتوى) يُقاس على **اتّحاد** الحزم
النشطة على المسارات الأربعة — و`deploy-to-play.ps1 -DryRun` يطبعها كلَّها في كتلةٍ واحدة.

⚠️ **ومسارُ الاختبار لا يُنسى بعد أن يُستعمل:** كلُّ رفعةٍ إلى `alpha`/`beta` تُبقي حزمتَها
نشطةً إلى الأبد ما لم تُستبدَل أو يُوقَف المسار. **وإسقاطُها لا يحتاج بناءً ولا رقماً جديداً**
— طريقان لا واحد: ترقيةُ الحزمة المنشورة إلى المسار، **أو إيقافُ المسار مؤقتاً**.

🟢 **والقرارُ المتّخذ 2026-09-07: الإيقافُ لا الترقية** — وهو ما يوصي به Play نصّاً (أدناه)،
ولا يمرّ بمسار الـAPI الساقط، ويُعاد تفعيلُ المسار لاحقاً بإصدارٍ جديد متى لزم.
**و`beta` بلا إصدارٍ نشطٍ أصلاً** فلا فعلَ عليه — ويُعاد فحصُ صفحة السياسة بعد إيقاف `alpha`
للتأكّد أن الحزمة `5` سقطت معها.
⚠️ **والخيارُ المرفوض يُكتب لأنه كان مختاراً ساعتين:** الترقيةُ إلى المسارين. سقط لسببين
مقيسين — الـAPI لا يصل Play على غير `internal`، وPlay يوصي بالإيقاف لا بالإبقاء.
🟢 **والمهلةُ ممدَّدةٌ بموافقة Play: 1 نوفمبر 2026** — البندُ
`play-target-api-warning-from-stale-test-tracks`.

🟢🟢 **وPlay نفسُه يكتب العلاجَ في «لإصدارك التالي» — قِرئ 2026-09-07 ولم يكن معلوماً ساعةَ
التخطيط:** «‏**يجب إيقاف مسار الاختبار المغلق القديم مؤقتاً لتجنُّب المشاكل المتعلّقة
بالسياسات أو مشاكل أخرى** — حلَّ إصدارٌ علنيٌّ محلَّ الإصدارات في مسار `alpha` منذ أكثر من
٩٠ يوماً. إذا لم يعد هذا المسار مستخدَماً، ننصحك بإيقافه مؤقتاً».
⇒ **«المشاكلُ المتعلّقة بالسياسات» هي هذا الإنذارُ بعينه**، والمُوصى به **الإيقافُ لا الترقية**.

⚠️ **وحالةُ المسارين ليست واحدة — والـAPI وحدَه لا يفرّق بينهما:**

| المسار | لوحةُ Play Console (‏قِرئت 2026-09-08) | جوابُ `Edits.tracks` |
|---|---|---|
| الاختبار المفتوح (`beta`) | **«غير نشط»** · «هذا الإصدار حلَّ محلّه إصدار آخر» | `Open Test - Version 1.2 (codes: 5) - completed` |
| الاختبار المغلق (`alpha`) | **«المسارات المتوقِّفة مؤقتًا»** · `Version 1.2 - Internal Test` · 13 أبريل | `(codes: 4) - completed` |

🔴 **وفخُّ قياسٍ يُسجَّل بثلاثة شواهدَ لا واحد: `completed` لا تشهد لشيء.**
**لا للتوزيع** — كانت جوابَ الـAPI قبل نشر `vc34` وبعده حرفياً · **ولا لنشاط المسار** —
`beta` **«غير نشط»** والـAPI يقول `completed` · **ولا لحالة الإيقاف** — `alpha` **موقوفٌ
مؤقتاً** بشهادة Console والـAPI يقول `completed` سواءً. ⇒ الحقلُ يصف **الطلب** وحده.

🟢 **وصار للفخّ علاجٌ مُنفَّذ لا وصفٌ فقط (2026-09-08):**
`.claude/skills/play-audit/track-state.json` يسجّل حالةَ المسار التي أقرّها المالكُ من
Console — **مُثبَّتةً برقم الحزمة**، فتُخفَّض النتيجةُ الحمراءُ إلى `ACKNOWLEDGED` مؤرَّخة،
و**أوّلُ رفعةٍ جديدةٍ إلى المسار تُبطل الإقرارَ ويعود الأحمرُ تلقائياً**. 🔴 **والسببُ أن
حارساً أحمرَ للأبد يُعلّم تجاهُلَ اللوحة** — وهو أخطرُ من غيابه. (ثلاثةُ ضوابطَ مقيسة في
`SKILL.md`، مخرَجُ كلٍّ مختلف.)

---

---

### 🔴 «النشرُ المُدار» مفعَّل — وكلُّ تغييرٍ يمرّ بثلاث خطواتٍ لا واحدة

قِيس 2026-09-07: إيقافُ `alpha` **سُجّل ولم يُطبَّق** — بقي في «التغييرات التي لم تُرسَل
للمراجعة بعد». ⇒ **① إرسالٌ للمراجعة ⇒ ② موافقةُ Google ⇒ ③ «نشر التغييرات»**،
ولا أثرَ لأيّ تغييرٍ قبل ③.

🔴 **وسيرةُ دليلٍ في يومٍ واحد — تُكتب كاملةً لأن الطريقَ هو الدرس:** قيل إن فخَّ
`completed` صار **«مُثبَتاً بالأثر»** بحجّة «أوقفنا `alpha` والحقلُ لم يتحرّك» ⇒ **سقطت
الحجّة**، لأن الإيقافَ **بقي في طابور الإرسال ولم يُطبَّق** ⇒ **قِيست نتيجةُ فعلٍ لم يُنفَّذ.**

🟢 **ثمّ كُسب الدليلُ بالتجربة الصحيحة في اليوم نفسِه:** الطلب **#21** أُرسل (4:29) ⇒ ووُفق
عليه (4:34) ⇒ و**نُشر (4:55)**. والـAPI بعده يقول **`[alpha] … (codes: 4) - completed`**
— **بلا أيّ فرقٍ عن قراءته قبل الإيقاف**. ⇒ **`Edits.tracks` لا تفرّق مسارَ اختبارٍ حيٍّ من
موقوف، مُثبَتاً بالأثر.**

🔴 **والدرسُ الباقي أعمُّ من النتيجة: «فعلتُه» ليست «نفَذ».** وقياسُ الأثر قبل الخطوة
الثالثة **يقيس لا شيء** — وهو ما جعل الدعوى الأولى باطلةً وهي تبدو مقيسة.

🟢 **والصفحةُ نفسُها قوّت براءةَ النشر المُدار بضابطٍ موجب:** تغييرٌ من Console **ظهر فيها
فعلاً** كبندٍ ينتظر ⇒ لو وصلت ترقيةُ الـAPI وحُجبت لظهرت كذلك. لم تظهر ⇒ **التحريرُ لا يصل
Play أصلاً**، وهو تشخيصٌ يزداد رسوخاً لا يهتزّ.

آخر تحديث: 2026-09-08 — الجولةُ الثانية (📋 **لوحةُ Play كاملةً ⇒ ستّةُ تصحيحات**:
🔴 «٠٫٠٠٪ ولا أعطالَ» **بطَلت** ⇒ **٠٫٠٣٪** أعطال و**٠٫٠٣٪** ANR ومشكلتان **غيرُ مؤرشفتين**
— كلتاهما على `vc31` وحدَه و«**٠ إصدار متاح حالياً**» ⇒ **لا شيءَ عاجل**، والفعلُ الوحيد
قراءةُ نصّ الاستثناء (فحصُ سلامةٍ لأن `vc35` غيّر `MainActivity` بعينها). · **عدّادٌ رابع
للتثبيتات: ٦٦٧**. · 🔴 **`vc34` نُشر 6:21م لا 4:51م** — تلك وقتُ **الإرسال**. · 🎯 **مرشّحُ
«ص/م»** يسبق مرشّحَ المنطقة الزمنية في تفسير التباين. · 🟢🟢 **وحُسم مصدرُ تحذير
edge-to-edge**: Play سمّى الأربعة — **مصدران Material سقطا في `vc35`**، والباقي
`androidx` = تنفيذُ `enableEdgeToEdge()` **الذي يوصي به Play نفسُه** ⇒ **صفرٌ من كودنا**.
· ⚠️ **ونصيحةُ R8** تُقرأ بحدّها: `isShrinkResources` **مفعَّلٌ أصلاً**. · 🟢 **وقراران:**
`vc35` تُشحن كما هي · وAGP 9 **مؤجَّلةٌ بندَاً**.)

آخر تحديث سابق: 2026-09-08 (🟢🟢 **زال إنذارُ `targetSdk` عن الحساب** — «تمت معالجة الانتهاكات»
· 2026-09-07 1:07م. و**`alpha` وحدَه كان الجاني**: مُدرَجٌ اليوم تحت «المسارات المتوقِّفة
مؤقتًا»، بينما `beta` **«غير نشط» أصلاً** ⇒ **الدعوى «`beta` يحتاج إيقافاً» تسقط بالقياس**.
⚠️ **وتباينٌ زمنيٌّ لا يُفسَّر بالتخمين**: 1:07م تسبق نشرَ الإيقاف (4:55م) ⇒ السببيّةُ
مرجَّحةٌ لا مُثبَتة. · 🔴 **وفخُّ `completed` صار بثلاثة شواهد** — لا للتوزيع ولا لنشاط
المسار ولا لحالة الإيقاف. · 🟢 **وله علاجٌ مُنفَّذ**: `track-state.json` يخفّض إقرارَ المالك
إلى ⚠️ مؤرَّخة **ويُبطله تغيُّرُ رقم الحزمة** — بثلاثة ضوابطَ مخرَجُ كلٍّ مختلف. · 🔴
**و`vc35` ما زالت محجوزةً** بضابط شريط التقدّم، **والعائقُ الأوّلُ الآن أذونُ حساب الخدمة**.)

آخر تحديث سابق: 2026-09-07 (🟠 **`vc35/3.2` بُنيت ووُقِّعت وأُرشفت** — `sha256 a1d86753e099…`
· رقمُها **غيرُ محروق** · **ولم تُرفع**، وضابطُ شريط التقدّم على جهازٍ ما زال معلَّقاً.
🔴 **وبناءُ 09-06 16:19 لا يُخلَط بها**: كودُ `vc35` بمانيفستِ `34/3.1`، والفارقُ **بايتان**
⇒ البصمةُ وحدها تفرز. · 🟢 **وعيبُ `-Promote` أُصلح** — الاسمُ والملاحظاتُ من **Play** لا من
الشجرة، بثلاثة ضوابطَ مخرَجُ كلٍّ مختلف. · 🧰 **ومهارةُ `/play-audit` أُضيفت** إلى `.claude/`
المتتبَّع. · ⚠️ **وأذونُ حساب الخدمة**: ظهر **العدُّ «١٣»** لا التفصيل ⇒ المرشّحُ **ضَعُف
ولم يسقط**، والحسمُ من قسم «الإصدارات» وحده.)

آخر تحديث سابق: 2026-09-06 (🟢🟢 **`production` صار على `vc34/3.1`** — نُشر فعلاً ووصل
المستخدمين، والقناةُ التي حسمت **سجلُّ نشاط إرسال الطلبات**: `طلب #20 · 4:51م · مرحلة
الإنتاج · منشور`. وصُحّحت المواضعُ التي كانت تقول «لم يصل أحداً» — **تصحيحاً لا حذفاً**.
🔴 **ولا يُقرأ ذلك إثباتاً لمسار الرفع الآليّ**: النشرُ وقع بيد المالك في Console،
و§سقوط رفع Play قائمٌ والبندُ مفتوح. ⚠️ و«منشور» **بدءُ منحدَرٍ لا قفزة**.
· **وثلاثةُ تصحيحاتٍ سابقةٍ من لوحة Play تبقى**: النشرُ المُدار **بريء** بدليل سجلّ
الإرسال · لا أعطالَ تُلاحَق (‏٠٫٠٠٪ · والـANR الوحيد يعلنه Play غيرَ قابلٍ للإصلاح) ·
والتثبيتاتُ **٦٦١** لا ٦٦٣. **و`vc35` على `main`** ولم يُبنَ للرفع: إزالةُ تبعيّة
Material ⇒ الحزمة −٣٧٫٩٪ والأصنافُ −٣٢٪ وصفرُ `datepicker`.)

<!-- ci-audit-2026-08-29 -->

---

