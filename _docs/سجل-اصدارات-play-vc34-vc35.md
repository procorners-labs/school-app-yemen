# سجلُّ إصدارات Play — `vc34` و`vc35` وسقوطُ الرفع الصامت والأعطال

> 🔴 **هذا الملفُّ هو المالكُ الكاملُ لهذا السرد** — نُقل بحرفه من `CLAUDE.md` في 2026-09-12
> (‏الأسطر 108–539 هناك) بعقدِ «صفر فقد»: **ولا سطرَ منه يُنسَخ إلى `CLAUDE.md`**، فالنسختان
> تتباعدان بصمت وقد وقع ذلك على هذا الجهاز مراراً.
>
> **والحكمُ التنفيذيُّ وحدَه بقي هناك** في §«الإصدارُ القائم» — يُقرأ أوّلاً، ويُرجَع إلى هنا
> لِـ«لماذا» ولِمَا قِيس ومتى.

---

## تغييرات معلّقة بانتظار النشر (`main` محدَّث، Play Store ليس بعد)

> 🟢🟢 **حالةٌ حاكمة 2026-09-06: `vc34/3.1` نُشر فعلاً ووصل مستخدمي `production`.**
> **وسقط الحاجزُ الذي دام من 2026-08-31.**
>
> 🔴 **والدرجاتُ الثلاث تبقى مكتوبةً وإن تجاوزناها — فهي التي تُنتج «رقّيتُ ولم يتغيّر شيء»،
> وأوّلُ إصدارٍ قادمٍ يمرّ بها كلِّها من جديد.** النشرُ المُدار مفعَّل على هذا الحساب،
> فبعد الترقية يمرّ التغييرُ بثلاث حالات:
> ① **أُرسل** (طلب #20 · 2026-09-06 4:51م) ⇒ ② **وافقت Google** ⇒ «جاهز للنشر» (5:02م)
> ⇒ ③ **ويبقى محجوباً عن المستخدمين حتى يُضغط «نشر التغييرات»** في «نظرة عامة على النشر».
> 🟢 **و③ تمّت لـ`vc34`** — الطلبُ #20 نفسُه يُقرأ اليوم **«مرحلة الإنتاج · منشور»** في
> سجلّ نشاط إرسال الطلبات. ⇒ الحالةُ انتقلت، **والبنيةُ لم تتغيّر**.
>
> ⚠️ **والـAPI يعرض ① و② و③ على أنها `completed` سواءً** — فقراءةُ المسار **لا تفرّق** بين
> «مُعيَّنٌ ينتظر النشر» و«وصل الأجهزة». 🔴 **وهذا الدرسُ ازداد إثباتاً لا نقصاناً:**
> `[production] v3.1 (codes: 34) - completed` كان جوابَ الـAPI **قبل النشر وبعده حرفياً**،
> بقياسين منفصلين. ⇒ **`completed` لا تشهد للتوزيع أبداً، ولا يُبنى عليها حارس.**
> **ومصدرُ الحقيقة للثالثة سطحان في Play Console وحدهما:** «نظرة عامة على النشر»
> (وعلامتُها تاريخُ «آخر نشر») و**سجلُّ نشاط إرسال الطلبات** — وهذا الأخير هو الذي حسم.
>
> 🟢 **وطلبُ #20 يُثبت التشخيص السابق بأثرٍ رجعيّ:** الطريقُ اليدويُّ أنشأ طلبَ إرسال،
> **ومحاولاتُ الـAPI الخمس لم تُنشئ ولا طلباً واحداً** ⇒ لم تصل Play أصلاً. (وسجلُّ الإرسال
> كان يقول ذلك قبل الترقية: آخرُ طلبين #19 يوليو و#18 يونيو.)
> 🔴 **ولا يُقرأ من النشر أن السكربت صار يعمل** — النشرُ وقع **بيد المالك في Console**،
> والـAPI ما زال يسقط صامتاً على `production`. §سقوط رفع Play أدناه قائمٌ بحقّه.
>
> ⚠️ **والطرحُ منحدَرٌ لا قفزة — وهذا يسري الآن لا لاحقاً:** تحديثُ المستخدم غيرُ إجباريّ
> ⇒ يبقى `vc31` و`vc34` متعايشَين أياماً بنسبةٍ متغيّرة. **ولا مصدرَ آليَّ عندنا لنسبة من
> حدّثوا فعلاً** — الـAPI يعطي حالةَ المسار والحزم لا توزيعَ الإصدارات على الأجهزة.
> ⇒ **«منشور» تعني بدءَ المنحدَر، لا أن الـ٦٦١ صاروا عليه.**

### 🟢 `vc34` / `3.1` — رُقّي إلى `production` في 2026-09-06 (‏وكان على `internal` منذ 09-05)

**ما يحمله فوق `vc33`:** تقييدُ مطالبة App Links **بمسار المستأجر** (‏PR #32).
`DeepLinkActivity` كان يطالب بـ`yemenschoolz.com` و`www.` بـ`autoVerify` **بلا أيّ قيدِ
مسار** ⇒ يعترض الصفحةَ العامّة و`/pricing` و`/portal` و**روابطَ كلّ مدرسةٍ أخرى** على نطاقٍ
متعدّد المستأجرين. صار مقصوراً على **١٠ مسارات** مشتقّةٍ من عقد الوركر المقيس.

🔴 **ولهذا لم تُرقَّ `vc33`:** المطالبةُ غيرُ المقيَّدة **لا تُعكَس إلّا بإصدارٍ لاحقٍ يصل
المستخدمين** ⇒ شحنُها ثمّ إصلاحُها أغلى من حرقِ رقمِ إصدار. (وvc32/vc33 **لا تُرفعان أبداً**.)

**المقيسُ من الحزمة المرفوعة نفسِها** — لا من الشجرة ولا نقلاً عن `vc33`:
`jar verified` · بصمة مفتاح الرفع `11:E9:B0…FA:39` · `targetSdk=36` · **نشاطان مُصدَّران
بالضبط** · **١٠ مُدخَلات مسار** في مانيفست الحزمة الثنائيّ · **`getFcmToken` حاضرةٌ في dex**
(وهي بعينها ما كان غائباً عن `vc31` فأعدم الإشعارات) · `school-494822`×2 و`mkrh-181cd`×**0** ·
R8 إزالة **64.6٪** بصفرِ قواعد واسعة · وsha256 المرفوع = المحلّي حرفياً.

🟢 **والشرطان اللذان كانا يحجبان الترقية تحقّقا مقيسَين على جهازٍ حقيقيّ 2026-09-06:**
① **الضوابط المعاكسة مرّت:** `/pricing` و`/portal` و`/<مدرسةٍ-أخرى>` ⇒ **المتصفّح**.
   🔴 وهذا هو الضابط — لا فتحُ `/abdaawatmuaz` بنجاح. ضابطٌ لا يتغيّر مخرَجُه بين الصحيح
   والخطأ ليس ضابطاً.
② **إشعارٌ فعليٌّ وصل** جهازاً عبر «اختبار إشعار الهاتف» ⇒ الرمزُ صُكَّ بمشروعٍ يطابق
   `FCM_PROJECT_ID` وجسرُ `getFcmToken` يعمل. ⚠️ **وحدُّه يُقال:** إشعارُ **اختبار** لا
   **بثُّ خبرٍ** حقيقيّ؛ ونفسُ المسار، فالإثباتُ للخاصّية لا لمسار نشر الأخبار.

🟢 **ووصل المستخدمين في اليوم نفسِه — بيد المالك لا بالأداة.**
كان مكتوباً هنا حتى مساء 2026-09-06: «🔴 **ومع ذلك لم يصل `vc34` أحداً** … `production`
ما زال `31`» — **وصار بائتاً، فصُحّح ولم يُحذف**. الترقيةُ اليدويّة في Play Console
أنشأت **الطلب #20** (4:51م)، ويُقرأ اليوم **«مرحلة الإنتاج · منشور»**.
🟢 والبندُ `vc34-promotion-to-production-blocked` **مغلقٌ** تبعاً لذلك.

🔴 **ويُفصَل الشقّان وإلّا طُوي العطلُ مع الحالة:** ما بطَل هو **حالةُ الإصدار**؛ وما
**لم** يبطُل هو أن **محاولتَي الترقية الآليّتين سقطتا صامتتين** (‏مع `releaseNotes`
وبلاها) وأعلنتا النجاح كذباً. ⇒ §سقوط رفع Play أدناه **قائمٌ بحقّه**، والبندُ
`play-upload-silently-dropped-root-unmeasured` **مفتوحٌ**: الجذرُ لم يُعزَل، ووصولُ
`vc34` **ليس شهادةً لـ`deploy-to-play.ps1`** لأنه لم يكن الفاعل.
🟢 **ولا رقمَ حُرق في شيءٍ من ذلك** — الترقيةُ لا ترفع ملفّاً.

⚠️ **و`vc34` رُفع إلى `internal` بلا ملاحظات إصدار** — انظر §الرفع أدناه.

### 🟠 `vc35` / `3.2` — بُنيت ووُقِّعت وأُرشفت 2026-09-07 · **ولم تُرفع**

> 🟢 **حُدّث 2026-09-07:** كان العنوان «لم يُبنَ للرفع» وصار بائتاً.
> `Workspace\Releases\SchoolAppyemen\SchoolApp-v3.2-vc35-2026-09-07.aab` ·
> `sha256 = a1d86753e099…` · **٣٬٣٣٤٬٧٣٠ ب** · `jar verified` ·
> بصمةُ مفتاح الرفع `11:E9:B0…FA:39` (تطابق `vc34`).
> 🟢 **ورقمُ `35` غيرُ محروقٍ عند Play** — قِيس في نفس اليوم: `next free: 35`.
>
> **والمقيسُ من الحزمة المؤرشَفة نفسِها لا من الشجرة:** `com.google.android.material`
> **غائبةٌ كلّياً** (‏و`datepicker` و`MaterialCalendarGridView` معها) · `getFcmToken`
> **حاضرةٌ في dex** · `school-494822`×1 و`mkrh-181cd`×**0** · **١٠ مُدخَلات مسار**
> App Links · وR8: أصناف **٣٬٠٦٦** · إزالة **٧١٫٨٪** · **صفرُ قواعدَ واسعة**.
>
> 🔴 **وبناءٌ سابقٌ لا يُخلَط بها ولا يُؤرشَف تحت أيّ اسم:** حزمةُ 09-06 16:19 تحمل
> **كودَ `vc35`** ومانيفستَها يقول **`34/3.1`** — بُنيت قبل رفعِ الرقم في الشجرة
> بعشر دقائق (الكوميت `6e491a0` عند 16:29). ⇒ **تحت `vc34` تكذب وتحت `vc35` تكذب.**
> والفارقُ بينهما بايتان (‏٣٬٣٣٤٬٧٢٨ مقابل ٧٣٠) ⇒ **الحجمُ لا يفرزها، والبصمةُ وحدها تفرز.**
> (الحادثةُ مسجّلةٌ سلفاً في `Releases\README.md`، وأُعيد الوقوعُ فيها في هذه الجلسة
> **ثمّ كشفها القياس قبل التسليم** — وهو ما يُقصد بـ«الأثرُ المبنيُّ يشهد على نفسه».)
>
> 🔴 **وضابطُ الجهاز ما زال معلَّقاً ⇒ لا تُرفع بعد** — انظر آخرَ هذا القسم.

**ما يحمله فوق `vc34`** — كلُّه مقيسٌ من الحزمة المبنيّة لا من الشجرة:

| | `vc34` | `vc35` |
|---|---|---|
| أصنافٌ في الحزمة | ٤٬٥٠٦ | **٣٬٠٦٦** (‏−٣٢٪) |
| منها `com.google.android.material` | ١٬٠٩٢ | **٠** |
| منها `…material.datepicker` | ٧٣ | **٠** |
| حجمُ الـAAB | ٥٬٣٧٢٬٠٧٣ ب | **٣٬٣٣٤٬٧٢٨ ب** (‏−٣٧٫٩٪) |
| نسبةُ إزالة R8 | ٦٤٫٦٪ | **٧١٫٨٪** |
| قواعدُ `-keep` واسعة | ٠ | ٠ |

🔴 **والسببُ الجذريُّ كان تبعيّةً بلا مقابل، لا إعداداً خاطئاً:** `com.google.android.material`
كان لها **مرجعٌ واحد** في المستودع كلِّه (`LinearProgressIndicator` في `activity_webview.xml`)
و**صفرُ استيرادٍ** في أيّ `.kt`، بينما كودُنا نحن ٣٤ صنفاً. وقاعدةُ
`-keep class com.google.android.material.** { *; }` كانت تُثبّت المكتبةَ كاملةً.

⚠️ **وتضييقُ القاعدة وحدَه لم يكفِ — قِيس على مرحلتين وهذا هو الدرس:** حذفُ `-keep`
أسقط ١٬٠٢٩ صنفاً لكنّ **٣٢ صنفَ `datepicker` نجت**، لأن **قاعدةَ المكتبة المستهلَكة نفسِها**
تُبقي `MaterialCalendarGridView` فيتبعها الباقي (‏`configuration.txt:81`) — **ولا تسقط بأيّ
قاعدةٍ نكتبها نحن**. ⇒ **الحلُّ إزالةُ التبعيّة، وبديلُها نمطٌ قائمٌ في المستودع**:
`MainActivity.kt:184` يبني `ProgressBar(…, progressBarStyleHorizontal)` لنفس الغرض أصلاً،
فالتبديلُ **وحّد الشاشتين** بدل أن يفرّق بينهما.

🟢 **وتحذيرُ Play «واجهاتٌ متوقّفة نهائياً» سقط مصدرُه المُسمّى** — كان يسمّي
`MaterialDatePicker.onStart`، والصنفُ لم يعد يُشحن.
⚠️ **وحدُّ الادّعاء يُقال:** السلسلتان `setStatusBarColor` و`setNavigationBarColor` **ما زالتا
في الـdex**، ومصدرُهما الآن `androidx.activity.EdgeToEdgeApi23/26/28` — أي **تنفيذُ
`enableEdgeToEdge()` نفسِه، وهو ما يوصي به Play**. ⇒ لا يُدَّعى اختفاءُ التحذير كلِّه؛
يُقاس بعد وصول الإصدار.
🟢🟢 **وحُسم الشقُّ الأكبرُ 2026-09-08 — Play سمّى المصادرَ بنفسه على `v3.1`:**

```
android.view.Window.setStatusBarColor · setNavigationBarColor · LAYOUT_IN_DISPLAY_CUTOUT_MODE_SHORT_EDGES
المواضع: c.p.t · c.q.t · c.s.t
         com.google.android.material.bottomsheet.BottomSheetDialog.onCreate
         com.google.android.material.internal.EdgeToEdgeUtils.applyEdgeToEdge
         androidx.appcompat.app.AppCompatActivity.<init>
         androidx.core.view.i.s
```

🟢 **ومصدران من الأربعة `com.google.android.material.*` — وقد سقطا في `vc35`** فعلاً:
`com.google.android.material` = **صفرٌ** في خريطة `vc35` وفي dex‑ها معاً.

🎯 **والثلاثةُ المُعتَّمة تُحلّ من خريطة `vc35`** — وكلُّها `androidx.activity`، **وصفرٌ من
كودنا**:

| Play | يُحلّ إلى |
|---|---|
| `c.p` | `androidx.activity.ComponentDialog$$ExternalSyntheticApiModelOutline0` |
| `c.q` | `androidx.activity.ComponentDialog` |
| `c.s` | `androidx.activity.EdgeToEdgeApi23` |
| (‏`c.v` — فرضُ 09-07) | `androidx.activity.EdgeToEdgeApi28` |

🔴 **وحدُّه يُقال ولا يُتجاوز:** الحلُّ من خريطة **`vc35`** وPlay سمّاها لـ**`vc34`**،
**وأسماءُ التعتيم تتبدّل بالبناء** ⇒ **مؤشّرٌ قويٌّ لا برهان**. (وخريطةُ `vc34` **لم تعد
متاحةً محلّياً** — دهسها بناءٌ لاحق؛ ودرسُه أن **خريطةَ كلّ إصدارٍ يُرفع تُؤرشَف معه**.)

🟢 **والخلاصةُ صلبةٌ باستقلالٍ عن التعتيم:** كلُّ المصادر غير Material **تنفيذُ
`enableEdgeToEdge()` نفسِه** — وهو **ما يوصي به Play في التحذير الآخر حرفياً**
⇒ **تحذيرٌ لا يُزال بكودنا، وشقُّه القابلُ للإزالة أُزيل في `vc35`.** يُعاد قياسُه بعد وصولها.

**وثلاثةُ تحسيناتٍ في الأداء، كلٌّ من علّةٍ مقيسة:**
① `checkForUpdates()` خرج من مسار الإقلاع الحرِج — كان يفتح ملفَّ تفضيلاتٍ ثالثاً ويقرأ ستّةَ
   مفاتيح متزامنةً **قبل** `loadUrl` · ② تجريدُ `Log.v/d/i` من الإصدار (‏٣٠ نداءً حيّاً كانت
   تُشحن) + حارسُ `BuildConfig.DEBUG` حول `onConsoleMessage` وهو **الأسخنُ**: يُنفَّذ لكلّ
   رسالةِ كونسول تصدرها الصفحة · ③ ذاكرةٌ وسيطة لروابط `AppConfig` ولرمز FCM — **بإبطالٍ
   صريحٍ عند كلّ كتابة**.

🔴 **وضابطٌ معاكسٌ لم يُشغَّل بعد، ولا يُعلَن النجاحُ بدونه:** شريطُ التقدّم يجب أن **يظهر
فعلاً** عند تحميل صفحةٍ في شاشتَي المعلّم والطالب على جهاز. تضييقُ R8 وتبديلُ عنصرِ واجهةٍ
ينكسران **صامتَين وقتَ التشغيل لا وقتَ البناء**.

#### ⚠️ ونصيحةُ R8 من Play على `v3.1` — تُقرأ بحدّها لا حرفياً (2026-09-08)

يوصي Play بـ«تحسين الأداة R8» ويسرد: تحسينٌ **٣١٪** · إخفاءُ مفاتيح **٣٢٪** · تقليصُ رموزٍ
**٣١٪** · **«تقليصُ الموارد المحسَّن غير مفعَّل»** · وترقيةُ **AGP إلى ٩٫٠ فأحدث**.

🔴 **وثلاثةُ حدودٍ تمنع قراءتَها حكماً:**
① **الأرقامُ لـ`vc34`** (‏`اسم الإصدار: v3.1`)، و`vc35` رفع الإزالةَ إلى **٧١٫٨٪** بمقياسنا
   ⇒ **مقياسان مختلفان لا يُقارنان مباشرةً**، ويُعاد قراءةُ التحذير بعد وصول `vc35`.
② **`isShrinkResources = true` مفعَّلٌ عندنا أصلاً** (‏`app/build.gradle.kts`) ⇒ مقصدُ Play
   «التقليصُ **المحسَّن**» علمٌ آخر (‏`android.enableResourceOptimizations`) **غيرُ مضبوط**
   في `gradle.properties`. **الخلطُ بينهما يقود لاستنتاج «الإعدادُ ناقص» وهو مضبوط.**
③ **AGP عندنا `8.13.2`** مقابل ٩٫٠ المطلوب.

🟢 **وقرارا المالك 2026-09-08 — يُكتبان قراراً لا إهمالاً:**
**`vc35` تُشحن كما هي** (حزمةٌ مقيسةٌ مختومةٌ مؤرشَفة؛ وإضافةُ علمٍ تجريبيٍّ الآن تُبطل كلَّ
ما قِيس عليها وتلزم قياساً كاملاً من جديد) · **وترقيةُ AGP 9 تُؤجَّل بنداً مستقلّاً**
(‏تطبيقٌ منشورٌ بمئات التثبيتات — تحتاج جلستَها ببناءٍ واختبارٍ كاملين).

### 🟢🟢 حُسم 2026-09-08: الجذرُ **أذونُ حساب الخدمة** — والترقيةُ بالـAPI نجحت أخيراً

**المقيس:** بعد منحِ `play-publisher@school-494822…` أذونَ الإصدار (‏سجلُّ الأنشطة:
`8 سبتمبر 15:37` · «أذونات الحساب: تم تغيير 5» و«أذونات التطبيق: تم تغيير 3»)،
جرت `-Promote -Track alpha -VersionCode 34` فصار **`alpha` يحمل `34`** — بقراءةٍ
**مستقلّةٍ بعد ٦٠ ثانية**، وبتأكيدٍ من `/play-audit` بعدها.
🟢 **وهذه أوّلُ ترقيةٍ تنجح بالـAPI على مسارٍ غير `internal`** بعد **٤ سقطاتٍ صامتة**.

🔴 **ودرسٌ في القياس أغلى من النتيجة — وقعتُ فيه في نفس الجلسة:**
**المحاولةُ الأولى بعد منحِ الأذون سقطت أيضاً**، فأعلنتُ «الأذونُ بريئةٌ بالأثر».
**وكان إعلاناً سابقاً لأوانه:** أذونُ Play **لا تنتشر لحظياً إلى الـAPI**، والمحاولةُ
الناجحة جرت بعد ~١٥ دقيقة بلا أيّ تغييرٍ آخر مقصود.
⇒ **قِستُ أثرَ تغييرٍ لم ينتشر بعد** — وهي فئةُ «**فعلتُه ليست نفَذ**» المكتوبةُ سلفاً في
§النشر المُدار، ولم أتعرّف عليها لأنها جاءت في **ثوبٍ آخر**: هناك طابورُ إرسال، وهنا
انتشارُ أذون. **والقاعدةُ تُعمَّم: بعد أيّ تغييرِ أذونٍ عند Play، أمهِل ثمّ أعِد القياس؛
وسقوطٌ واحدٌ بعد التغيير لا يُبرّئ شيئاً.**

⚠️ **وحدٌّ لا يُطوى — لم يُعزَل أيُّ إذنٍ بعينه:** مُنِحت الأذونُ **دفعةً واحدة** وفيها
🔴 **«المشرف (جميع الأذونات)»**. ⇒ **لا يُعرَف أكانت أذونُ الإصدار الثلاثة كافيةً وحدَها**.
**والعزلُ مجانيٌّ ومتاح:** يُسحب «المشرف» ويبقى الثلاثة، ثمّ تُرقّى `34` إلى `beta`
(‏وهو ما زال يحمل الحزمة البائتة `5`) ⇒ **فعلٌ واحدٌ يُثبت الكفاية ويُسقط آخرَ حزمةٍ بائتة معاً**.

🎯 **ومرشّحٌ سادسٌ سقط في الطريق، وأعطى معلومةً أثمنَ من نجاحه:** إضافةُ
`?changesNotSentForReview=true` إلى `Edits.commit` ردّت **400**:
«‏Changes are sent for review **automatically**. The query parameter … must not be set».
⇒ **التحريرُ يصل Play ويُتحقَّق منه**، والمراجعةُ تلقائية ⇒ **النشرُ المُدار لم يكن يحجب
الـAPI أصلاً** — تأكيدٌ مستقلٌّ لبراءته المسجَّلة سلفاً.

🟢 **وأثرٌ دائمٌ في الأداة:** `tracks.update` و`commit` كانا يُلقيان جسمَ الردّ في متغيّرٍ
**لا يُقرأ** ⇒ بقيت «‏Assigned» و«‏committed» **شهادتين جوفاوين في ٤ سقطات**. صارا
يُطبعان، وصارت رسالةُ الالتزام: «‏returned OK (**NOT proof**)».

---

### 🔴 وسقوطُ رفع Play الصامت استُنسخ 3/3 — السجلُّ الكامل (‏حُسم أعلاه)

`deploy-to-play.ps1` طبع `AAB uploaded · versionCode: 34` ثمّ `Assigned to track` ثمّ
`Edit committed successfully` — **ولا شيء منها بقي**. قياسٌ مستقلٌّ بعد **٤٥ ثانية**:
المسارُ ما زال على `33`، و`34` غائبٌ عن قائمة الحزم ⇒ 🟢 **الرقم لم يُحرق**، و🔴 **ليس
تحقّقاً سابقاً لأوانه**.

**والجسمُ الذي نجح** (‏`vc34` صار حيّاً به) يختلف في ثلاثة: ① حقلُ **`track` كان غائباً**
من جسم `Edits.tracks.update` — والوثائقُ تنصّ أن الجسمَ **موردُ Track** يحمله مع `releases`،
والمقطعُ في الـURL لا يُغني · ② `versionCodes` **سلسلةٌ لا عدد** · ③ بلا `releaseNotes`.
🔴 **والجذرُ لم يُعزَل:** ثلاثةُ متغيّراتٍ معاً تُثبت أن أحدَها السبب **ولا تقول أيَّها**.
⚠️ **والفرضُ المسجَّل فيه سابقاً منقوضٌ لا مؤجَّل:** كان يتّهم تمريرَ `versionCodes` **نصّاً**
— والمقيسُ أن **النصَّ هو الناجح** والعددَ هو الساقط.

#### 🔴 وسقطتان جديدتان 2026-09-06 — عزلٌ نقضَ فرضَه، وتقديرُ كلفةٍ انهار

رُقّي `vc34` إلى `production` مرّتين بنفس الجسم العامل على `internal`: **مع**
`releaseNotes` ثمّ **بلاها**. **سقطتا كلتاهما** — «‏Assigned» ثمّ «‏committed»، ثمّ
`production` **كان** ما زال `31` بقراءةٍ **مستقلّةٍ** بعد ٦٠ث و٧٥ث.
⇒ 🔴 **`releaseNotes` بريئة**، والفرضُ الثالث سقط كسابقَيه.
⚠️ **وهذا وصفُ لحظةِ المحاولتين لا حالةُ اليوم** — `production` صار على `vc34` مساءَ
التاريخ نفسِه **بالطريق اليدويّ**، والسقطتان باقيتان كما هما: **لم تصل Play أصلاً.**

🟢 **وانهار تقديرُ الكلفة المكتوبُ أعلاه:** «العزلُ يكلّف ثلاثَ رفعاتٍ = ثلاثةَ أرقامٍ
محروقة» **صحيحٌ للرفع وباطلٌ للترقية** — الترقيةُ لا ترفع ملفّاً فلا تستهلك رقماً
⇒ **العزلُ مجانيٌّ في وضع `-Promote`**، وقد جرى بلا كلفة. لا يُحتجّ بالكلفة بعد اليوم.

🎯 **وما تبقّى متغيّرٌ واحد: المسارُ نفسُه.** الشكلُ عينُه **نجح على `internal`** وسقط
على `production` مرّتين ⇒ العلّةُ **خاصّةٌ بالمسار لا بالجسم**. والمرشّحان لا يُقاسان من
الـAPI بل من Play Console بيد المالك: **النشرُ المُدار** (يلتزم ولا ينشر، **ولا يسري على
`internal`** ⇒ يطابق المقيسَ حرفياً) · و**المسوّدةُ الفارغة** القائمة على `production`.
🔴 **ولم يُجرَّب `beta` كضابطٍ فاصل** عمداً — مسارٌ حيٌّ له مختبرون، وتغييرُه أثرٌ خارجيٌّ
غيرُ مأذون. **التشخيصُ لا يبرّر فعلاً لم يأذن به المالك.**

🟢 **وعُطلٌ ثالثٌ كان يُخفي هذا كلَّه، أُصلح في PR #33:** خطوةُ «‏Discovering current app
state» تضرب `applications/<pkg>/tracks` **بلا مقطع `edits/<editId>`** ⇒ **404 في كلّ تشغيلة**،
ورسالتاها «‏app may be new on Play» و«‏No bundles found yet (first upload)» **تدعوان لاستنتاجٍ
خاطئ بثقة** في تطبيقٍ منشورٍ بـ٦٦١ تثبيتاً. ⇒ الفحصُ الوحيد الذي يكشف رقماً محروقاً **قبل
عمليةٍ لا رجعةَ فيها** كان يعطي عكسَ الحقيقة.

#### 🟢 والنشرُ المُدار **بريء** — نُقض الفرضُ الرابع 2026-09-06، ومعه تصحيحُ تراجعٍ منّي

كان **المرشّحَ الأقوى** في هذا الملفّ وفي `pending.json`. وحين أكّد المالك أنه **مفعَّل**
تراجعتُ فوراً وقلت إن الترقية «نجحت على الأرجح ودخلت المراجعة». 🔴 **والتراجعُ نفسُه كان
سابقاً لأوانه** — ثمّ حسمه **سجلُّ نشاط إرسال الطلبات** في Play Console: آخرُ طلبين
`#19 · 17 يوليو 2026` و`#18 · 10 يونيو 2026`، و**لا طلبَ يومَ محاولاتِ الـAPI إطلاقاً**.
فلو كانت الترقيةُ التُزمت وحُجبت بالنشر المُدار لظهر طلبٌ ينتظر.
⇒ **لم يصل الطلبُ أصلاً.**

⚠️ **ونصُّ القياس أعلاه صار يحتاج تأريخَه — لا نقضَه:** كان مكتوباً «**لا طلبَ اليوم
إطلاقاً**»، وهو **صحيحٌ في لحظته** ومقيسٌ قبل الترقية اليدويّة. ثمّ أنشأ المالكُ
**الطلبَ #20** يدوياً في **4:51م من اليوم نفسِه**. ⇒ **البائتُ صيغةُ الحاضر وحدها؛
والاستنتاجُ باقٍ صحيحاً بلا مساس** — لأنه عن **محاولات الـAPI** لا عن اليوم:
الخمسُ لم تُنشئ ولا طلباً، والطلبُ الوحيد أنشأه الطريقُ اليدويّ.
🔴 **وهذا بعينه ما يجعل الفرضيةَ أدناه قائمة:** الأداةُ لم تُثبَت سليمةً بالنشر،
لأنها لم تكن الفاعلة فيه.

**والدرسُ المستخلَص أعمُّ من الحادثة: تأكيدُ المالك على وجود آليّةٍ ليس دليلاً على أنها هي
الفاعلة.** «النشرُ المُدار مفعَّل» صحيحةٌ تماماً و**لا تعني** أنه سببُ هذا العطل. القرينةُ
الحاسمة كانت سجلّاً يُقاس، لا حالةَ إعدادٍ تُؤكَّد.

#### 🎯 وعزلٌ مقيسٌ 2026-09-07: العلّةُ ليست في `production` — بل في **كلّ مسارٍ غير `internal`**

`-Promote -Track alpha -VersionCode 34` (‏بروفةٌ نظيفة ثمّ تنفيذٌ فعليّ) طبع
«‏Assigned to track: alpha» ثمّ «‏Edit committed successfully» — و**`alpha` ما زال `4`**،
بقراءتين: خطوةُ التحقّق الداخلية في السكربت فوراً، ثمّ `-DryRun` **مستقلٌّ بعد ٦٥ ثانية**.
🟢 **ولا رقمَ حُرق ولا مسوّدةٌ خُلّفت** — الترقيةُ لا ترفع ملفّاً، والقراءةُ التالية أظهرت
المسارات الأربعة كما كانت بلا إصدارٍ زائد.

**والحصيلةُ عبر المسارات الأربعة تفصل فصلاً حاسماً:**

| المسار | الترقيةُ بالـAPI | العدد |
|---|---|---|
| `internal` | 🟢 **نجحت** | ١/١ |
| `alpha` | 🔴 سقطت صامتةً | ١/١ |
| `production` | 🔴 سقطت صامتةً | ٢/٢ |

🔴 **ويُقلَب بذلك تشخيصان كانا مكتوبين هنا:**
① **«العلّةُ خاصّةٌ بـ`production`» بطَلت** — الحدُّ الفاصل هو `internal` مقابل ما سواه.
② **ومرشّحُ «المسوّدة الفارغة على `production`» سقط** — لا مسوّدةَ على `alpha` وسقط سواءً.

**واتُّهم النشرُ المُدار ساعةً** لأنه يسري على `alpha` و`beta` و`production` ولا يسري على
`internal` ⇒ يطابق الجدولَ **٤/٤**. 🔴 **وبرئ بقياسٍ في نفس اليوم — من سطحه الخاصّ هذه
المرّة:** «نظرة عامة على النشر» ⇒ **«التغييرات الجاهزة للنشر» فارغة**، وسجلُّ الإرسال يقف
عند **`#20`** (‏6 سبتمبر · الإنتاج · منشور) بلا طلبٍ لاحق.
⇒ **لو التُزمت ترقيةُ `alpha` وحُجبت لظهرت في أحد السطحين.** لم تظهر في أيٍّ منهما.

🔴 **وهذا يقلب سؤالَ العطل كلَّه: التحريرُ لا يصل Play أصلاً.** `Edit committed successfully`
**كذبٌ محض** لا «التزامٌ حُجب عن النشر» — ولا أثرَ للطلب في أيّ سطحٍ من سطوح Console.

🎯 **والمرشّحُ الباقي — ويطابق الجدولَ ٤/٤ كذلك، ويفسّر الصمتَ بدل أن يفترضه: أذونات حساب
الخدمة.** Play يفصل أذونَ الإصدار **ثلاثةً مستقلّة** لا واحداً:
`Release to internal testing` · `Release to testing tracks` (‏المغلق والمفتوح) ·
`Release to production`. ⇒ حسابٌ يملك **الأوّلَ وحده** ينتج بالضبط ما قِيس: نجاحٌ على
`internal`، وسقوطٌ على الثلاثة الباقية.
🔴 **ويُقاس بيد المالك في دقيقة** (‏ولا يُقرأ من `androidpublisher`): Play Console ←
**المستخدمون والأذونات** ← `play-publisher@school-494822.iam.gserviceaccount.com` ←
أذوناتُ التطبيق. البندُ `play-upload-silently-dropped-root-unmeasured` **يبقى مفتوحاً**
حتى ذلك — **مرشّحٌ قويٌّ ليس قياساً**.

⚠️ **وقياسٌ جزئيٌّ 2026-09-07 أضعفَ المرشّحَ ولم يُسقطه — ويُكتب بحدّه لا أقوى:** فتح
المالكُ صفحةَ الحساب فأظهرت **«أذوناتُ التطبيق … مدارس الإبداع والتميز الدولية · ١٣»**،
أي **١٣ إذناً ممنوحاً على مستوى التطبيق** — وهو **قريبٌ من إجمالي أذون التطبيق عند Play**،
فيرجّح أن الثلاثةَ ممنوحةٌ جميعاً. 🔴 **لكنّ الشاشة عرضت العدَّ لا التفصيل**، ولم يظهر أيٌّ
من `Release to production` / `testing tracks` / `internal testing` مُعلَّماً بعينه.
⇒ **العددُ ليس تفصيلاً، والفرضُ لا يُنقَض بعددٍ إجماليّ.** الحسمُ بنقرِ صفِّ التطبيق
وقراءةِ قسم «الإصدارات» وحده.

🔴 **ولو سقط هذا المرشّحُ أيضاً فالمساحةُ لا تفرغ** — يبقى بعده: نطاقُ OAuth للتوكن
المستعمَل · وسياسةٌ على مستوى الحساب تُقيّد الكتابةَ بالـAPI على المسارات المنشورة.
**تُكتب الآن لئلّا يُقرأ سقوطُ المرشّح الخامس نهايةَ الطريق.**

#### 🔴 وعيبٌ ثانٍ انكشف في وضع `-Promote`: اسمُ الإصدار وملاحظاتُه من الشجرة لا من الحزمة

جسمُ الترقية المُرسَل حمل `"name": "v3.2"` وملاحظاتِ **`vc35`** — بينما المُرقَّى `vc34/3.1`.
مصدرُهما `build.gradle.kts` وقتَ التشغيل، وهو اليوم على `versionCode 35`.
⇒ **لو نجحت الترقية لوصل مختبرو `alpha` إصداراً اسمُه ووعودُه ليست له** (‏«الوضع الليلي»
و«إشعارات فورية» موصوفةٌ لإصدارٍ لم يُرفع أصلاً).
🟢 **ولم يقع الضررُ لأن الترقية سقطت** — صدفةٌ لا ضابط. وهي فئةُ «الأثرُ المبنيُّ يشهد على
نفسه، والشجرةُ لا تشهد عليه»: `-Promote` **لا يلمس ملفّاً** فلا يجوز أن يشتقّ وصفَه من شجرةٍ
تقدّمت عنه.

🟢 **وأُصلح 2026-09-07 — والمصدرُ صار Play لا الشجرة.** في وضع الترقية يُبحث في جواب
`Edits.tracks` عن الإصدار الحامل للرقم المطلوب، ويُؤخذ منه `name` و`releaseNotes`.
🔴 **ولا هبوطَ صامتٌ إلى الشجرة عند العجز** — يُرفض ويُسمَّى البديلُ `-ReleaseName`،
لأن الهبوطَ هو العطلُ بعينه: يُنتج وصفاً خاطئاً بلا رسالةٍ واحدة. ووضعُ الرفع يبقى على
الشجرة، وهي هناك **شاهدٌ صحيح** لأن الحزمة تُبنى منها الآن.

**والضوابطُ الثلاثة مقيسةٌ ومخرَجُ كلٍّ منها مختلف** (‏بروفاتٌ محضة، ولا مسوّدةَ تُترك):

| المُدخَل | المخرَج |
|---|---|
| `-VersionCode 34` (على `production` و`internal`) | 🟢 `Release name resolved FROM PLAY: v3.1` — **لا `v3.2`** |
| `-VersionCode 33` (مرفوعٌ بلا مسار) | 🔴 رفضٌ يسمّي `-ReleaseName` بدلاً من الهبوط |
| `-VersionCode 35` (غيرُ مرفوع) | 🔴 رفضٌ مبكّر قبل أيّ فعل |

⇒ البندُ `promote-uses-tree-release-notes-not-bundle` **مغلق**.

#### ⚠️ الأعطال — **قِيست 2026-09-08 وصار ما كان مكتوباً بائتاً**

🔴 **كان هنا: «نسبةُ الأعطال ٠٫٠٠٪ · وصفحةُ الأعطال تعطي (لا نتائج) · والبندُ الوحيد
مؤرشَفٌ وهو ANR».** **بطَل الثلاثةُ معاً** — يُصحَّح ولا يُحذف، لأن مصدرَ بطلانه هو الدرس:
**رقمُ لوحةٍ يُجمَّد في وثيقة يبقى مقروءاً بثقةٍ بعد أن يكذب.**

| | المقيسُ 2026-09-08 |
|---|---|
| نسبةُ الأعطال التي لاحظها المستخدمون | **٠٫٠٣٪** (‏`+٠٫٠٣٪` عن الـ٢٨ يوماً السابقة) |
| نسبةُ أخطاء ANR | **٠٫٠٣٪** (‏`−٠٫١٠٪`) |
| المشكلاتُ المعروضة | **اثنتان — وغيرُ مؤرشفتين** |

**والاثنتان على `31 (2.8)` وحدَه، وPlay يقول عن كلٍّ منهما نصّاً:**
**«تؤثّر هذه المشكلة في ٠ إصدار متاح حاليًا للتنزيل»** — و`vc31` **استُبدل 6 سبتمبر 6:21م**.

| المشكلة | النوع | الحجم |
|---|---|---|
| `MainActivity.onCreate` ⇒ `android.util.AndroidRuntimeException` | تعطُّل | حدثٌ واحد · مستخدمٌ واحد · قبل ٧ أيام |
| `MessageQueue.nativePollOnce` ⇒ «Input dispatching timed out (No focused window)» | ANR | حدثٌ واحد · مستخدمٌ واحد · قبل ١٢ يوماً |

🔴 **والـANR لا يُلاحَق** — سبق أن أعلنه Play غيرَ قابلٍ للإصلاح («سلسلة التعليمات الرئيسية
غير نشطة»). 🟢 **ولا يُؤجَّل إصدارٌ لأجل أيٍّ منهما.**

⏱️ **والفعلُ الوحيدُ المستحَقّ — دقيقتان:** قراءةُ **نصّ الاستثناء الكامل** من صفحة العطل.
🔴 **وسببُه فحصُ سلامةٍ لا ملاحقةُ عطل:** `vc35` غيّر **`MainActivity` بعينها** (تبديلُ
عنصر التقدّم · نقلُ `checkForUpdates` · تضييقُ R8) ⇒ يلزم التأكّدُ أن العطلَ ليس من فئةٍ
قد يوسّعها.

🎯 **وفرضيّةٌ تُقال فرضيّةً لا حكماً:** `AndroidRuntimeException` في `onCreate` لتطبيقٍ كلُّه
WebView **غالباً** `WebViewFactory$MissingWebViewPackageException` — أي **«Android System
WebView» معطَّلٌ أو قيد التحديث على جهاز المستخدم**، وهي **حالةُ جهازٍ لا عيبُ كود**.
🔴 **ولا يُبنى عليها قرار قبل قراءة النصّ** — والفئةُ واسعة. وإن تأكّدت ⇒ التقاطُ الاستثناء
وعرضُ رسالةٍ أصليّة، **في `vc36` لا في `vc35`**: لا يُدسّ تغييرٌ في حزمةٍ مقيسةٍ مختومة.

⚠️ **وحدُّ القراءة يبقى:** اللوحةُ وصفحةُ الأعطال **نافذتاهما وفلاترُهما مختلفة** —
ويُسمّى مصدرُ كلّ رقمٍ معه.

---

- **2026-07-11 — جسر تعطيل SwipeRefreshLayout الأصلي عند فتح الدرج الجانبي (منصة المعلم).** مدموج
  في `main` (`SchoolJsBridge.kt`/`BaseWebViewActivity.kt`/`MainActivity.kt`)، `compileDebugKotlin`+
  `assembleDebug` نجحا محلياً، الطرف المقابل في الويب (`school-app-yemen-gas`) **منشور حيّاً بالفعل**
  (`teacher@552`) وآمن بلا أي أثر إنتاجي حتى تُثبَّت نسخة أندرويد جديدة. **لم يُرفَع Play Store بعد
  بطلب صريح من المالك** (يريد إضافة تعديلات أخرى أولاً وينشر بنفسه لاحقاً). التفاصيل الكاملة + سبب
  الخلل الجذري + قائمة اختبار يدوي كاملة قبل/بعد الرفع في
  [`_docs/2026-07-11-swiperefresh-sidebar-bridge.md`](_docs/2026-07-11-swiperefresh-sidebar-bridge.md)
  — راجعه أولاً قبل أي محاولة نشر لاحقة لهذا التغيير تحديداً.
- **2026-07-17 — تحديث الروابط الافتراضية إلى `school.procorners.com` + سقالة Firebase Cloud
  Messaging (FCM) وصوت التنبيهات + تحسينات Dark Mode/أمان.** راجع
  `C:\Users\osama\.claude\plans\snug-wishing-balloon.md` للخطة الكاملة وحالة كل مرحلة.
  **ما أُنجِز وبُني محلياً بنجاح (`compileDebugKotlin`):** `AppConfig.kt` (روابط + trustedSslDomains)،
  `SchoolApplication.kt`/`SchoolFcmService.kt` جديدان (قناة إشعارات بصوت مخصّص `res/raw/notify_chime.wav`
  + عرض إشعار)، Dark Mode لصفحة الخطأ (`WebViewSupport.errorPageHtml` عبر `prefers-color-scheme`) +
  الثيم صار `Theme.AppCompat.DayNight.NoActionBar` مطبَّقاً فعلياً في المانيفست (كان يُطبَّق
  `Theme.AppCompat.Light.NoActionBar` مباشرة بدل الثيم المخصّص — ثغرة سابقة)، `mixedContentMode`
  شُدِّد من `ALWAYS_ALLOW` إلى `COMPATIBILITY_MODE`، `SchoolAppyemen.zip` القديم (7MB) أُزيل من git.
  **مؤجَّل عمداً (خطر انحدار بلا جهاز حقيقي للتحقّق):** دمج `MainActivity` مع `BaseWebViewActivity`
  (توسيع بدل تكرار) — تغيير معماري على شاشة الإقلاع (LAUNCHER) بلا إمكانية اختبار حيّ في هذه الجلسة؛
  وتضييق `cleartextTrafficPermitted` (لا يزال `true` عاماً) — نفس السبب. كلاهما بحاجة جلسة قادمة مع
  اختبار جهاز فعلي قبل التنفيذ.
  **لا يُنشر Play Store قبل:** (أ) تدوير كلمة مرور الـkeystore الحقيقية، (ب) اختبار FCM حيّ على
  جهاز حقيقي، (ج) تنفيذ قائمة اختبار جسر PTR أعلاه.

  🔴 **وموضعُ كلمة السرّ صُحِّح بالقياس 2026-09-11 — كان مكتوباً هنا اسمان خطأ، وهو يُضلّل من يتتبّعه:**
  كان النصّ يقول إنها في `check-signing.ps1`/`diagnose-signing.ps1`. **والمقيسُ بعدِّ ورودِ
  السلسلة في الثلاثة: `check-signing.ps1` ⇒ **٠** · `diagnose-signing.ps1` ⇒ **٠** ·
  **`build-release.ps1` ⇒ ٢** (سطرا ٣٤ و٣٦ — رسالةُ مساعدةٍ تطبع قالبَ `keystore.properties`).
  🔴 **وثقبٌ ثانٍ كُشف في نفس المرور، وهو أخطرُ لأنه يُسكت الحارس:** استثناءُ
  `android-keystore-password` في `.gitleaks.toml` كان يُدرج تلك السلسلةَ بعينها ضمن
  «القيمِ التافهة» ⇒ **الحارسُ يُخضِرّ على السرّ الحقيقيّ لأنه يُشبه النائب.** أُسقطت
  من الاستثناء، **والنوائبُ العامّةُ بقيت.**
  🟢 **وأُزيلت من الشجرة في نفس اليوم** — صارت `<see Workspace\Secure\>`، ومعها صُحِّح مسارُ
  `storeFile` البائت (‏`C:/Users/osama/schoolapp.jks` ⇒ `Workspace/Secure/schoolapp.jks`).
  ⚠️ **وإزالتُها من الشجرة لا تُبطل ما في التاريخ** — المستودعُ **عامّ**، فالتدويرُ في اللوحة
  يبقى هو الفعلَ الوحيد الذي يُغلق البند (‏`rotate-keys`).

---

