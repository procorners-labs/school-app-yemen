# صحّةُ الخادم بعد نشر `vc36` — 2026-09-17 · البطءُ والأخطاءُ من طابور GAS لا من التطبيق

> جلسة `schoolappyemen-d2`. **قياسٌ فقط** — لا تعديلَ في `SchoolApp` ولا `SchoolApp-gas` من هنا
> (جلسةٌ واحدة لكلّ مستودع · و`wrong-root-guard` يحجب ذلك بحقّ).
> يُكمل: [`2026-09-17-قياس-الجهاز-usb-وسلسلة-عطل-الخبر.md`](2026-09-17-قياس-الجهاز-usb-وسلسلة-عطل-الخبر.md).

## ① الإصدار

`/play-audit` (قراءةٌ مستقلّة): **`production` = `36 (3.3)`** · `internal` = 36 · `alpha` = 34 (**`!!` أقدمُ من
الإنتاج** — `targetSdk 36` فلا إنذارَ مطابقة، ويصير «غير نشط» تلقائياً) · الرقمُ الحرّ **37**.
⚠️ اسمُ إصدار الإنتاج «`36 (3.3)`» لا «`v3.3`» — الترقيةُ تمّت من Console. **تجميليٌّ لا سلوك.**

## ② الأخطاء — سجلّاتُ الوركر `school-teacher-proxy` · آخرُ ٢٤ ساعة (حتى `2026-09-17T01:30Z`)

| الحالة | العدد | `wallTime p95` |
|---|---|---|
| 200 | 4,925 | 15,052ms |
| **502** | **758** | **23,706ms** |
| 304 | 420 | 3,700ms |
| **0** (انقطاع/استثناء) | **262** | 18,634ms |
| 404 | 118 | 370ms |
| **503** | **112** | 23,999ms |

⇒ **~١٧٪ من الطلبات (١٬١٣٢ من ٦٬٦٢٩) لم تنجح**، و**p95 الـ502 = سقفُ ميزانية GAS (23,700ms) حرفياً**
⇒ **مهلاتٌ لا أعطالُ كود.**

## ③ من أين تأتي — `gasMs` لكلّ دالّة (٢٤ ساعة)

| الدالّة | النداءات | الوسيط | **بلغت السقف (≥23ث)** |
|---|---|---|---|
| **`getStudentBootBundle`** | 261 | **23,700** | **161 (٦٢٪)** |
| `getHomePageBundle` | 214 | 7,534 | 56 (٢٦٪) |
| `studentHeartbeat` | 176 | 7,325 | 48 |
| **`checkAppVersion`** | 141 | 5,389 | **45** |
| `loginStudent` | 129 | 8,750 | 36 |
| `autoSaveGradesBatchProtected` | 99 | 15,509 | 35 |
| **`getTeacherBootBundle`** | 51 | **23,700** | 34 (٦٧٪) |
| `getGrades` | 127 | 9,760 | 30 |
| `getHomeScheduleBundle` | 134 | 7,210 | 28 |
| `getStudentSchoolBrand` | 94 | 6,650 | 23 |
| `registerDeviceToken` | 60 | 8,441 | 21 |
| `getStudentReports` | 40 | 22,523 | 19 |
| `teacherHeartbeatProtected` | 71 | 7,421 | 19 |
| `saveGradesProtected` | 37 | 17,971 | 16 |

🎯 **الدليلُ الحاسم على أن العلّةَ طابورٌ لا كلفةُ دالّة:** `checkAppVersion` **تقرأ خاصّيةً واحدة** — ووسيطُها
**٥٫٤ث** و**٤٥ منها بلغت ٢٣ث**. دالّةٌ بهذا الحجم لا تستغرق ذلك إلّا منتظرةً دورَها في حدّ GAS
للتنفيذ المتزامن. ⇒ **كلُّ نداءٍ منخفضِ القيمة يسرق مقعداً من تحميل شاشة الطالب.**
وأحداثُ الوركر تؤكّده: `bulkhead reject` = **88** · و`apicache nokey` = **212** (نداءاتٌ لا مفتاحَ تخزينٍ لها).

## ④ ثقلُ الصفحات — من هذا الجهاز (٣ محاولاتٍ لكلٍّ · `br`)

| الصفحة | المنقول | غيرُ المضغوط | زمنُ أوّل بايت |
|---|---|---|---|
| **`/teacher/index.html`** | **578 ك.ب** | **2.15 م.ب** | 0.13–0.30ث |
| `/student/index.html` | 162 ك.ب | 518 ك.ب | 0.12–0.26ث |
| `/assets/Cairo-Variable.ttf` | 228 ك.ب | 600 ك.ب | 0.14–0.37ث |
| `/home/index.html` · `/abdaawatmuaz` | 69 ك.ب | 227 ك.ب | 0.12–0.83ث |
| بقيّةُ الصفحات والأصول | < 40 ك.ب | — | ~0.12–0.26ث |

🟢 **الصفحاتُ الساكنة سريعةٌ من الخادم.** 🔴 **وثقلُها على شبكة اليمن:** `teacher` **578 ك.ب** بـ
`Cache-Control: no-cache` ⇒ تُعاد مصادقتُها في كلّ فتح (`304` ظهر **420** مرّة، فالمصادقةُ تعمل جزئياً).
⚠️ **ومن هذا الجهاز زمنُ الخبر 0.12–0.25ث** مقابل **2.7–8.4ث من الهاتف** قبل ساعات ⇒ **زمنُ `getNewsOg`
يتبع ازدحامَ طابور GAS لحظتَها** — وهي الفئةُ نفسُها في §③.

⚠️ **حدُّ القياس:** انقطع الهاتفُ عن USB أثناء هذه الجولة ⇒ **جدولُ §④ من هذا الجهاز (مخرجُه فرنسا) لا من
شبكة اليمن**. والسجلّاتُ في §②–③ **مستخدمون حقيقيّون**.

## ⑤ خطّةُ الإصلاح — مرتّبةٌ بالأثر المقيس

| # | الإصلاح | المستودع | لماذا أوّلاً | النشر |
|---|---|---|---|---|
| **1** | **خدمةُ `checkAppVersion` من الوركر** (تخزينٌ بمفتاح الحزمة لساعة) — **صفرُ نداءِ GAS** | `SchoolApp` | ١٤١ نداءً/يوم لقيمةٍ ثابتة · ٤٥ مهلة | 🔴 `wrangler deploy` |
| **2** | **تخزينُ الدوالّ العامّة للقراءة** في `apicache`: `getStudentSchoolBrand` · `getTeacherSchoolBrand` · `listPartnerSchoolsPublic` · `getHomeScheduleBundle` · `getHomePageBundle` (بمفتاح المدرسة) | `SchoolApp` | ~٦٦٠ نداءً/يوم · `nokey` = 212 | 🔴 `wrangler deploy` |
| **3** | **حقنُ OG لزواحف المعاينة وحدها** | `SchoolApp` | يُسقط `getNewsOg` لكلّ زائرٍ بشريّ | 🔴 `wrangler deploy` |
| **4** | **تخفيفُ النبضات**: `studentHeartbeat` + `teacherHeartbeatProtected` (٢٤٧/يوم · ٦٧ مهلة) — تباعدٌ أطول أو دمجُها في نداءٍ قائم | `SchoolApp-gas` | يحرّر مقاعدَ الطابور لتحميل الشاشات | 🔴 `clasp deploy` |
| **5** | **`getStudentBootBundle` / `getTeacherBootBundle`**: قياسُ كلفتها داخل GAS منفصلةً عن الانتظار، ثمّ `CacheService` لكلّ مدرسة | `SchoolApp-gas` | ٦٢٪ و٦٧٪ مهلة — **أكبرُ أثرٍ على المستخدم** | 🔴 `clasp deploy` |
| **6** | **`sw.js`**: offline حقيقيّة · لا سقوطَ إلى الجذر · `CACHE v9` | `SchoolApp-gas` ⇒ CI | الصفحةُ الخطأ | 🔴 الدمجُ حيّ |
| 7 | ثقلُ `teacher/index.html` (2.15 م.ب): فصلُ الوحدات الكسولة | `SchoolApp-gas` | شبكاتٌ بطيئة | 🔴 |
| 8 | **البنيويّ:** برنامجُ الهجرة إلى `Supabase` قائمٌ ومالكُه `SchoolApp-gas/DATABASE_MIGRATION_PLAN.md` | — | الطابورُ حدٌّ في GAS نفسِه | قرارُ مالك |

🟢 **البنودُ ١–٣ في الوركر وحدَه وبلا مساسٍ بالـGAS** ⇒ **أسرعُ مكسبٍ وأقلُّ خطراً**، ويُقاس أثرُها بالاستعلام
نفسِه في §②–③ بعد ٢٤ ساعة.
🟢 **ولا يمسّ أيٌّ منها عقدَ المضيفات المجمَّد في الحزم**، و**«يمن سكولز» يستفيد منها كلِّها** — نفسُ الوركر ونفسُ GAS.
⚠️ **وما لم يُقَس هنا ولا يُدَّعى:** أنّ البنود ١–٣ وحدها تُسقط الـ502 تحت نسبةٍ بعينها — **يُقاس بعد النشر.**

## ⑥ الخاصّية

`ANDROID_LATEST_VERSION_CODE_com.proconrers.schoolappyemen` ⇒ **تُضبط الآن `36`** (3.3 ظاهرٌ في المتجر
بقول المالك · و`production = 36` بـ`/play-audit`) — في مشروع `teacher` ⇒ «خصائص النص البرمجي».

## ⑦ مطالبةٌ جاهزة — جلسة `SchoolApp` (‏`cc web`)

> اقرأ `C:\Users\osama\AndroidStudioProjects\SchoolAppyemen\_docs\2026-09-17-صحة-الخادم-بعد-نشر-vc36.md` §②–⑤ و`2026-09-17-قياس-الجهاز-usb-وسلسلة-عطل-الخبر.md` §③. أعِد القياسَ نفسَه من سجلّات `school-teacher-proxy` (٢٤ ساعة: الحالات · `gasMs` لكلّ `fn` · `gasMs ≥ 23000`)، ثمّ نفّذ البنود ١ و٢ و٣ من §⑤ على فرعٍ وPR واحدٍ لكلّ بند، مع ضابطٍ ثنائيّ القطب لكلٍّ (مثلاً: `checkAppVersion` تُخدَم بلا نداءِ GAS **وتتغيّر** حين تتغيّر الخاصّية بعد انقضاء التخزين). أعرض عليّ الفرقَ والضوابط، ولا `wrangler deploy` قبل موافقتي الصريحة. وبعد النشر بـ٢٤ ساعة أعِد الاستعلامات وقارن.

## ⑧ مطالبةٌ جاهزة — جلسة `SchoolApp-gas` (‏`cc gas`)

> اقرأ `C:\Users\osama\AndroidStudioProjects\SchoolAppyemen\_docs\2026-09-17-صحة-الخادم-بعد-نشر-vc36.md` §③ و§⑤ البنود ٤–٦. ابدأ بالبند ٥: قِس كلفةَ `getStudentBootBundle` و`getTeacherBootBundle` **داخل GAS** (زمنُ التنفيذ الفعليّ منفصلاً عن انتظار الطابور)، واقترح تخزينَها بـ`CacheService` لكلّ مدرسة مع إبطالٍ عند الكتابة. ثمّ البند ٤ (النبضات) والبند ٦ (`sw.js`). ES5 فقط. فرعٌ وPR لكلّ بند، ولا `clasp deploy` ولا دمجَ قبل موافقتي.
