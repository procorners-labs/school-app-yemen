# أرقامُ الإصدار والعدّادات — ومن يشهد للرقم

> 🔴 **هذا الملفُّ هو المالكُ الكاملُ لهذا السرد** — نُقل بحرفه من `CLAUDE.md` في 2026-09-12
> (‏الأسطر 702–789 هناك) بعقدِ «صفر فقد»: **ولا سطرَ منه يُنسَخ إلى `CLAUDE.md`**.
>
> **والحكمُ التنفيذيُّ وحدَه بقي هناك** في §«أرقامُ الإصدار» — والقياساتُ والعدّاداتُ ودروسُ
> القياس هنا.

---

## الإصدارات — المستودع كان متأخّراً إصداراً عن المنشور (صُولِح 2026-08-11)

| المصدر | القيمة |
|---|---|
| **Play (إنتاج) — اليوم** | `versionCode` **34** · `versionName` **3.1** · **نُشر 2026-09-06 6:21م** |

🔴 **وتصحيحٌ مقيس 2026-09-08 — خلطٌ بين حدثين كان مكتوباً في خمسة مواضع:** «4:51م» هي
وقتُ **إرسال الطلب #20**، لا وقتُ النشر. و**النشرُ 6:21م** بشاهدين متطابقين في لوحة
الإنتاج: «تاريخ الإصدار: 6 سبتمبر 6:21 م» · و«‏`31` … **تم استبداله** في 6 سبتمبر 6:21 م».
⇒ **الفجوةُ ساعةٌ ونصف بين الإرسال والنشر** — وهي بعينها الخطوةُ الثالثة في النشر المُدار.
**والمواضعُ الأخرى تبقى صحيحةً** لأنها تصف الطلبَ لا النشر.
| **Play (إنتاج) — قبله** | `versionCode` **31** · `versionName` **2.8** · نُشر 2026-07-17 07:40 · 4 بلدان |

🔴 **ولا يُنقَل «عددُ المستخدمين» من هذا الملفّ — Play يعرض ثلاثةَ عدّاداتٍ مختلفةِ الفلاتر،
والنقلُ الأعمى بينها وقع مرّتين مقيستين.** قِيست كلُّها 2026-09-07 في لوحةٍ واحدة:

| العدّاد | القيمة | ما يعنيه |
|---|---|---|
| «عمليات تثبيت التطبيق» | **٦٠٢** | لوحةُ «الاختبار والإصدار» |
| «عدد الأجهزة النشطة شهرياً» | **٥٧١** | لوحةُ المعلومات · نافذةُ ٢٨ يوماً |
| «تثبيتاتٌ نشطة» | **٦٦١** | قِيس 2026-09-06 — وكان يُروى **٦٦٣** في كلّ وثائقنا |
| «عمليات تثبيت» — **لوحةُ مرحلة الإنتاج** | **٦٦٧** | قِيس 2026-09-08 · بجوار «أحدث إصدار: v3.1 · ٤ بلدان» |

⇒ **الأربعةُ صحيحةٌ معاً ولا يصحّح أحدُها الآخر.** والخطأ أن يُقرأ أيٌّ منها «عددَ مستخدمينا».
🔴 **وظهورُ الرابع بعد يومين من كتابة «ثلاثة» هو الدليلُ لا الاستثناء:** كلُّ عدٍّ يُثبَّت
هنا يبطل، **والعنوانُ نفسُه لا يحمل عدداً** (فئةُ «رقمٌ في عنوانٍ يتقادم بصمت»).
⚠️ **ولا يُجمَّد رقمٌ هنا** — يُقرأ من اللوحة لحظةَ الحاجة، **ويُسمّى عدّادُه معه دائماً**.
| **هذا المستودع قبل التصحيح** | `versionCode` 30 · `versionName` "2.7" — والشجرة **نظيفة** |

⚠️ **والصفّان يتعايشان عمداً:** الطرحُ **منحدَرٌ لا قفزة** ⇒ `vc31` ما زال على أجهزةٍ
كثيرة، و**قيودُه** (‏§مضيفات الحزمة المنشورة أدناه) **حيّةٌ حتى ينتهي المنحدَر**.

🟢 **وصار للرقم مصدرٌ حيٌّ يُقاس بأمرٍ واحد منذ 2026-09-05 — فلا يُنقَل من وثيقة:**
```powershell
pwsh -File .\deploy-to-play.ps1 -Track internal -DryRun   # قراءةٌ محضة، ولا تترك مسوّدة
```
يطبع مسارات Play بإصداراتها **كلِّها**، وقائمةَ الحزم المرفوعة، و**الرقمَ التالي الحرّ**.
(كان يردّ 404 صامتاً حتى أُصلح — انظر §سقوط رفع Play أعلاه.)

🔴 **وحدُّه قاطعٌ ويُقرأ مع الجدول أعلاه: هذا مصدرٌ للرقم المحجوز، وليس مصدراً لـ«الحيّ».**
يقرأ الـ`API` (‏`Edits.tracks`)، والـAPI يجيب عن **حالة الطلب** لا **حالة التوزيع**:
`[production] v3.1 (codes: 34) - completed` كان جوابَه **قبل النشر وبعده حرفياً**،
بقياسين منفصلين 2026-09-06. ⇒ **لا يُبنى عليه حارسٌ آليٌّ لسطر «الحيّ» أبداً —
سيخضرّ على إصدارٍ لم يصل أحداً**، وذاك أغلى من وثيقةٍ بائتة: البائتةُ تُقرأ فيُشكّ
فيها، والحارسُ الأخضرُ يُصدَّق بلا قراءة.
🟢 **ومصدرُ «الحيّ» سطحان في Play Console وحدهما:** «نظرة عامة على النشر» (وعلامتُها
تاريخُ «آخر نشر») و**سجلُّ نشاط إرسال الطلبات** (يسمّي الطلبَ ومرحلتَه وحالتَه).
⚠️ **ولا يُقاسان آلياً عندنا** — يُقرآن بيد المالك، **وهذا حدُّ الأدوات لا نقصُ اجتهاد.**

أي أن `vc31` بُني ونُشر **دون أن يُسجَّل رفع الرقم في git**، فمن يقرأ `build.gradle.kts` وحده
يستنتج أن المنشور 2.7. (‏`versionName = 2.8` مُستخرَج من **مانيفست الحزمة نفسها** لا من اسم ملفها،
ومطابق لما يقوله Play — مصدران مستقلّان.)

🔴 **والقاعدة تصف العلاقة لا القيمة: الرقم التالي = آخرُ ما رُفع + 1، لا آخرُ ما نُشر + 1.**
Play يحجز أيَّ `versionCode` **رُفع** ولو بقي **مسوّدةً لم تُنشَر**، ويرفض إعادة استخدامه ولو لم
يعرفه المستودع — وخطؤه («رمز الإصدار مستخدَم») يبدو غامضاً بلا هذا السطر.

⚠️ **ولا يُكتب هنا رقمٌ بعينه.** كان مكتوباً «الإصدار القادم يبدأ من 32» فصار كذباً حين
استُهلك 32 (‏بُني ووُقِّع ورُفع مسوّدةً لم تُنشَر، 2026-08-12) وحين بُني 33 (‏2026-09-01).
⇒ **رقمٌ مشتقٌّ يُجمَّد في وثيقة يصير إملاءً بائتاً يُقرأ بثقةٍ ويُطبَّق، بينما غيابُه يدفع
للقياس.** الرقم يُقرأ من `app/build.gradle.kts` بعد `git pull`، ويُراجَع مقابل Play Console.
(الفئةُ نفسُها ضُبطت في تعليق `build.gradle.kts` بمراجعة `claude[bot]` على PR #23.)

> ⚠️ **ودرسٌ في القياس نفسه:** أوّل قراءة لهذا الرقم أُخذت من نسخة `main` محلّية **متأخّرة تسعة
> كوميتات**، فأعطت «29/2.6» وأنتجت ادّعاءً بأن الفجوة إصداران. الرقم الصحيح ظهر فقط بعد
> `git fetch` وقراءة `origin/main`. ⇒ **لا يُقرأ رقم إصدار من شجرة عمل لم تُحدَّث للتوّ.**

🔴 **والاتجاه المعاكس أخطر، وقِيس 2026-09-01: الكوميتُ الذي يحمل رقمَ الإصدار ليس بالضرورة
الذي بُني منه.** سُئلتُ «أيَّ نطاقٍ تشير إليه `vc31` المنشورة؟»، والطريقُ البديهيّ
`git show <الكوميت الذي فيه versionCode = 31>:…/AppConfig.kt`. **وهو يعطي شجرةً لم تُبنَ منها
الحزمةُ قطّ** — ذاك كوميتُ **مصالحةٍ** (‏`08c1d27`، 2026-08-11) كُتب بعد النشر بأربعة أسابيع،
والمستودعُ وقت البناء (2026-07-17) كان يقول `30`/`"2.7"`. **ولا وسمَ واحداً في المستودع**
(‏`git tag -l` فارغ) يسدّ الثغرة.

⇒ **رقمُ الإصدار وسمٌ يُكتب متى شاء الكاتب؛ والحزمةُ وحدها تشهد على نفسها. فحين يوجد أثرٌ
مبنيّ، يُقاس هو لا الشجرة:**
```bash
# فكّ الحزمة المؤرشَفة واقرأ منها — حضورٌ/غياب لا تكرار، والغيابُ وحده قاطع
unzip -q "Workspace/Releases/SchoolAppyemen/SchoolApp-v<n>-vc<c>-<date>.aab" -d /tmp/x
grep -ral "school.procorners.com" /tmp/x   # ⇒ base/dex/classes.dex
```
(‏`grep -c` على `classes.dex` يعدّ **سطراً واحداً طويلاً** لا مرّاتِ الظهور — لا يُقرأ تكراراً.)
والشجرةُ تبقى مصدراً مساعداً: **آخرُ كوميتٍ قبل ساعة البناء** (‏`git log -1 --until=…`) لا
الكوميتُ الحامل للرقم.

⚠️ **ودرسٌ عامّ يتبع: جوابٌ صحيحٌ بدليلٍ خاطئ يبقى قابلاً للانهيار عند أوّل مراجعة.**
النتيجةُ هنا لم تتغيّر والدليلُ تغيّر — وذاك فرقٌ يُكتب لا يُطوى.

