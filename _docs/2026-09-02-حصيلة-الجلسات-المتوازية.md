# حصيلةُ الجلسات المتوازية — 2026-09-01 → 09-02

> **ما هذه الوثيقة:** جمعٌ لآخر حالةِ كلِّ جلسةٍ عملت على هذا الجهاز في هذه النافذة، بأمر
> المالك. كُتبت من جلسة `schoolappyemen-88` (‏جذرُها `AndroidStudioProjects\SchoolAppyemen`).
>
> 🔴 **وقاعدةُ قراءتها — تسبق كلَّ رقمٍ فيها:** ما تحت §١ **قِستُه بنفسي بأداته**؛ وما تحت §٢
> **نقلٌ عن أصحابه ولم أُعِد قياسَه** ولا أملك أدواتِ بعضِه أصلاً (‏`clasp` · سجلّاتُ Cloudflare ·
> `pending.json` · Play Console). **والنقلُ عن نظيرٍ ليس قياساً ولو كان النظيرُ صادقاً** —
> وقعت هذه العلّةُ **ثلاث مرّاتٍ في هذه النافذة** وأُصلحت ثلاثَها.
>
> ⇒ **أيُّ رقمٍ في §٢ يُعاد قياسه عند مصدره قبل البناء عليه.**

---

## §١ · ما قِستُه بنفسي — `school-app-yemen` (أندرويد)

**الحالة:** `main@437e6ab` · شجرةٌ نظيفة · **صفرُ PR مفتوح**
(‏مُتحقَّقٌ بـ`git ls-remote` لا بـ`git rev-list @{u}..HEAD` — الأخيرُ يقارن بمرجعٍ محلّيّ
يبقى بعد اختفاء الفرع البعيد فيكذب؛ **ودمجُ squash يُبطل `merge-base --is-ancestor` أيضاً**.)

### خمسةُ PRs مدموجة — مُتحقَّقةٌ بـ`gh pr view --json state`

| # | دمج | ماذا |
|---|---|---|
| **23** | `0c42356` | `vc33` / `3.0` — بناءٌ وتوقيعٌ مُتحقَّقان |
| **24** | `5771f62` | 🔴 **إصلاحُ فشلٍ صامتٍ في `deploy-to-play.ps1`** |
| **25** | `6415080` | وثيقةُ التسليم |
| **26** | `5414d93` | قيدا الإصدار: مشروعُ FCM · ومضيفاتُ الحزمة المجمَّدة |
| **27** | `437e6ab` | تضييقُ مدى أثر App Links |

### ما نُشر

🟢 **`vc33` على مسار `internal` فقط** — بإذنٍ مباشرٍ من المالك بعد استشارةِ أربع جلسات.
مُتحقَّقٌ **خارجياً** بأداةٍ مستقلّة لا بمخرَج السكربت: `internal status=completed versionCodes=33`.
🔴 **و`production` لم يُمَسّ** (`versionCodes=31`).

### 🔴 الفشلُ الصامت — أهمُّ ما وقع في هذه الجلسة

`deploy-to-play.ps1` طبع `Edit committed successfully` ثمّ `DEPLOY COMPLETE / Status: Published`
وخرج **بصفر** — و`vc33` **لم يكن على Play إطلاقاً**. مُستنسَخ **٢/٢**، ومقيسٌ ثلاثَ مرّاتٍ
بفواصل ٤٥ ثانية. ودليلٌ مستقلٌّ ثانٍ: **الرفعُ الثاني قَبِل `33` بلا شكوى «رمز مستخدَم»**.

**العلّتان — تجتمعان ولا تكفي إحداهما:**
① وجهةُ التحقّق `/applications/{pkg}/tracks/{track}` **ليست نقطةَ نهايةٍ صالحة** (القراءةُ
تلزمها جلسةُ تحرير) ⇒ **404 دائماً ⇒ الحارسُ لم يفشل مرّةً: لم يجرِ مرّةً**، منذ كُتب السكربت.
② و`catch` يبتلعه بـ`WARN`، والملخّصُ يطبع `Published` **بلا شرط**.

🟢 **أُصلح ودُمج** بجلسةِ قراءةٍ صحيحة و`exit 1` عند عدم التأكيد، **وبضابطٍ معاكس على نفس
منطق الاستعلام**: `internal`⇒33 **أخضر** · `alpha`⇒4 **أحمر**.
⚠️ **وجذرُ سقوط الرفعتين مرشَّحٌ لا مقيس** — متغيّران: `versionCodes` نصّاً لا عدداً · أو
`releaseNotes` عربية في `pwsh -File`. **تجربتان تعزلانه، ولم تُجريا.**

### فحصُ الأسرار — والمستودعُ **عامّ**

```
service-account-key.json   🟢 على القرص · غيرُ متتبَّع · صفرُ كوميتِ إضافةٍ في كلّ التاريخ
keystore.properties        🟢 المثل
*.jks · *.keystore         🟢 صفرُ كوميتِ إضافةٍ في كلّ التاريخ
app/google-services.json   ⚠️ متتبَّع — وليس تسريباً
```
🟢 **والأصفارُ حقيقيةٌ لا فشلَ بحث:** نفسُ الأمر على `google-services.json` أعاد **`1`** ⇒ ضابطٌ موجب.
**ولماذا ليس تسريباً:** مفاتيحُه تُشحن داخل كلّ APK بحكم التصميم — أُثبت بالأثر: استُخرج
`project_id`/`project_number` من `base/resources.pb` في **الحزمة المنشورة `vc31`**.
🔴 **لكنّ الحمايةَ في التقييد لا في الإخفاء: هل المفتاحُ مقيَّدٌ ببصمة SHA-1 واسمِ الحزمة في
لوحة GCP؟ لم يُقَس — بندٌ مفتوحٌ بمالكٍ `user`.**

### قياساتٌ على الحزم نفسِها — لا على الشجرة

| السؤال | الجواب المقيس |
|---|---|
| أيَّ نطاقٍ تضرب `vc31` المنشورة؟ | خمسةُ مسارات على `school.procorners.com` فقط: `home\|teacher\|student\|cms/index.html` و`gas/teacher` |
| وهل تطلب `procorners.com` الجذر؟ | **لا** — السلسلةُ حاضرةٌ في الـdex لكنها في `trustedSslDomains` (تجاوزُ SSL) **لا وجهةَ طلب** |
| و`schedule/index.html`؟ | **في المصدر وغائبٌ عن الحزمة** — أزاله R8 ⇒ **من يحرسه يحرس عدماً** |
| وهل في `vc31` روابطُ تطبيق؟ | **لا** — صفرُ `action.VIEW`/`autoVerify`/`DeepLinkActivity` (بضابطٍ موجب: `MESSAGING_EVENT` حاضرٌ في الحزمتين) |
| ومشروعُ Firebase؟ | `vc31` و`vc33` كلتاهما `school-494822` / `108410742569` · صفرُ أثرٍ للشقيق |
| وهل استُهلك `vc32`؟ | **نعم** — قائمةُ حزم Play: `1,2,3,4,5,11,26,27,28,29,31,32,33` |

### R8 والتحقّق على جهاز

إزالة **64.6%** · **صفرُ قاعدةٍ واسعة** · و`SchoolFcmService`/`FirebaseMessagingService` في
`seeds.txt` مع `onNewToken`/`onMessageReceived` باقيَين في `mapping.txt`.
وعلى محاكٍ **API 35** بحزمة **release** موقَّعةٍ بمفتاح الرفع: المضيفان `verified` ·
و**١٢/١٢** مسبارَ توجيه · **وطفرتان أثبتتا أن المسبار يحمرّ** (توقُّعٌ خاطئ ⇒ فشل · ورابطٌ
يبتلعه تطبيقُنا تحت `NOT_US` ⇒ فشل).

---

## §٢ · ما نُقل إليّ — لم أُعِد قياسَه

> 🔴 كلُّ ما في هذا القسم **بعهدة قائله**، ويُعاد قياسه عند مصدره.

| الجلسة | جذرُها | آخرُ حالةٍ أبلغتها (‏آخرُ تحديثٍ 2026-09-02 ~13:1xZ) |
|---|---|---|
| `fix-teacher-dashboard-tables` | `SchoolApp-gas` | `main@2c6186a` · ستّةُ PRs مدموجة · `#1391` ينتظر CI · **`teacher@893` منشورةٌ ومُتحقَّقة** · صفرُ عملٍ غيرِ منشور. ⚠️ **وهذا رقمُها هي وقد تجاوزه المستودع** — انظر الصفَّ التالي |
| `schoolapp-gas-c7` | `SchoolApp-gas` | **لم تبدأ** — صفرُ تعديلٍ وصفرُ نشر. وقاست `main@543ce97` ⇒ **أحدثُ من رقم جارتها** (‏`#1393` دُمج بعده) |
| `fix-schoolapp-www-verification` | `SchoolApp` | **`main@93db68d`** · شجرةٌ نظيفة · صفرُ PR مفتوح · **اثنا عشرَ PR مدموجاً** (#166…#177) · و**واحدٌ فقط نُشر حيّاً**: #166 |
| `complete-schedule-grid-sync` | الجذر | **٥٧ كوميتاً** في اليومين · مدفوعةٌ ومُتحقَّقة · **🔴 صفرُ نشر** · و`pending.json` مالكُه: **٨٦ مفتوحاً من ١٦٨** · **٣٩ أُغلق بنتيجةٍ مقيسة** · **٤٧ بمالكٍ `user`** |
| `osama-23` | الجذر | **لم تبدأ** — صفرُ كتابةٍ وصفرُ نشر |
| `rukn-tasawuq-1e` | `rukn-tasawuq` | فرعُ `fix/docs-shop-and-origin-latency@385fb72` · **PR #10 مفتوحٌ لا مدموج** (`mergedAt: null`) · صفرُ `clasp` — **لكن ثلاثَ كتاباتٍ حيّةٍ على Cloudflare** |

🔴 **وفخُّ نقلٍ ضُبط في هذا الجدول نفسِه — يُقال لأنه سيتكرّر:** كتبتُ أوّلاً «‏`main@a69daac`
وخمسةُ PRs» و«صفرُ قاعدةِ كاشٍ نُفِّذت» — **وكلاهما بائتٌ ساعةَ الكتابة**: صار ١٢ PR، ونُفِّذت
ثلاثُ كتاباتٍ حيّة. ⇒ **حالةُ جلسةٍ حيّةٍ تتقادم بين السؤال والتوثيق**، فالجدولُ يحمل
**طابعَ لحظتِه** ولا يُقرأ حاضراً.

### تصحيحان جوهريّان وصلا بعد المسودّة

**① `rukn-tasawuq` نشر فعلاً — «صفرُ نشر» كان خطأً في وصفي.** ثلاثُ كتاباتٍ حيّة على
Cloudflare بإذن المالك في نافذتها، كلُّها مقيَّدةٌ بمضيفَي المتجر ولها مسارُ تراجعٍ موثَّق:
تضييقُ `/shop` بـ`http.host` · وإغلاقُ ثغرة استثناء الكاش · ولا ثالثة.
🟢 **وثغرةُ `/cart` مقابل `/cart/` أُغلقت** — الاستثناءُ صار `path in {"/cart" "/userlogin"}`
مع `starts_with` للباقي و`/my-wishlist`.
⇒ **الدمجُ ليس النشرَ الوحيد: قاعدةُ حافّةٍ تُكتب مباشرةً بلا كوميت.**

**② 🔴 والأخطرُ في هذه النافذة كلِّها — عطلٌ حيٌّ في أصل المتجر:**
```
١٨٫٥٪ من ٣٬١٥٩ طلباً/٣ ساعات ⇒ 5xx
524 × ١٠٢  ·  متوسّطُ زمن الأصل ١٢٥٬١٤٨ ms
499 × ٣٠٥  ⇒ زوّارٌ غادروا قبل الردّ
```
⇒ **ليس بطئاً بل فقدانَ خدمة.** ومالكُه المالك (تذكرةُ `sellvir`). **وهذا يُصحّح توصيفي
السابق «صفحتان ثقيلتان»** — الصفحتان صحيحتان **والعطلُ أوسع منهما.**

### ثلاثةُ نتائجَ نُقلت إليّ ولها أثرٌ على قراراتي — وسمتُها كذلك

1. 🔴 **`FCM_PROJECT_ID` مضبوطةٌ في `master-admin` الذي لا يقرؤها، وغائبةٌ عن `teacher` الذي
   يقرؤها** ⇒ **البثُّ خامدٌ ٢٠ يوماً وكلُّ المؤشّرات خضراء**. القاعدةُ المشتقّة: **اتبع
   القارئَ لا المُعلِن** — `appsscript.json` يُعلن النطاق (إذن) و`PropertiesService` يحمل
   القيمة، **ولا يعيشان في نفس المشروع بالضرورة**. ⇒ **لا يُوصَف `vc33` بأنه «يُفعّل
   الإشعارات» في أيّ ملاحظات إصدار.**
2. 🔴 **صلاحياتٌ مفتوحة:** `Master_Admin_School` وشيتُ مدرسةٍ حيّة **`{"role":"writer","type":"anyone"}`**
   ⇒ مَن يملك الرابط يُعدّل ويحذف بلا تسجيل دخول. **فئةُ العطل: إذنٌ يُمنَح بشرطٍ على حالةٍ
   متغيّرة ولا يُعاد تقييمُه يبقى على أوسعِ قيمةٍ بلغها.**
3. ⚠️ **`procorners.com` — ثلاثةُ توصيفاتٍ متعاقبة، وكلُّ واحدٍ نقض سابقَه:**
   «استضافةٌ ساقطة» ⇐ «صفحتان ثقيلتان» ⇐ **والمستقرّ: عاملان معاً والجذرُ غيرُ محسوم.**
   الأدلّةُ التي فرضت الاستقرار: **أرضيةٌ بطيئة** (‏`/robots.txt` = **٣٬٩٣٣ ms** لملفٍّ ثابت
   ⇒ بطءٌ عامٌّ لا صفحيّ) · **وتذبذبٌ ضخم** (‏١٣٬١١١ / ٣٤٬٦٥٤ / ٥٢٬٨٠٠ ms **لنفس الصفحة في
   ساعة**) · **وفقدانُ خدمةٍ فعليّ** (‏١٨٫٥٪ 5xx · `524`×١٠٢ بمتوسّط أصلٍ ١٢٥٬١٤٨ ms).
   ⇒ وُسِم `procorners-slow-gradient-and-variance` **بالعرَض المقيس والجذرُ حاجزٌ مفتوح عمداً**.
   🔴 **ودرسُه أهمُّ من رقمه: ثلاثةُ توصيفاتٍ خاطئة كلُّها كانت «معقولةً» ومبنيّةً على قياسٍ
   صحيحٍ لعيّنةٍ ناقصة.** والذي حسم لم يكن قياساً أدقَّ للصفحة الثقيلة بل **قياسَ ملفٍّ
   ثابتٍ تافه** — أي **توسيعَ العيّنة لا تعميقَها**.
   ⚠️ وسطرُ هوك الصحّة **صادق**، لكنه يفحص الرئيسية بمهلة ٣٠ث ⇒ **يقيس عتبةً على أسوأ
   عيّنة**، لا الموقعَ كلَّه.

---

## §٣ · دروسُ القياس — كلٌّ من عطلٍ وقع في هذه النافذة

### السلسلةُ الخماسية: فجوةٌ بين فعلٍ وأثرِه

> «التزمتُ» ≠ حُفظ · «فعلتُ» ≠ سجّلتُ · «دمجتُ» ≠ نشرتُ · «فتحتُ PR» ≠ أُصلح · **«رُفعت» ≠ وصلت**

**والجامعُ في سطر: كلّما فُصل الإعلانُ عن التحقّق.** وظهرت في **ثلاث طبقات**: مهمّةٌ مجدولة
(`Ready` تُخفي فشلاً) · وركفلو CI (خطوةٌ تُخضِرّ بصفر مراجعة) · **وسكربتُ نشر** (`Published`
بلا رفع).

### قواعدُ مقيسة

| القاعدة | من أين |
|---|---|
| **الكوميتُ الحاملُ لرقم الإصدار ليس الذي بُني منه** ⇒ حين يوجد أثرٌ مبنيّ، يُقاس هو لا الشجرة | كوميتُ «مصالحةٍ» بعد النشر بأربعة أسابيع كاد يُعطي جواباً خاطئاً بثقة |
| **رقمٌ مشتقٌّ يُجمَّد في وثيقة يصير إملاءً بائتاً** يُقرأ بثقةٍ ويُطبَّق | «التالي يبدأ من 32» في **ملفّ تعليمات المراجِع الآلي** |
| 🔴 **الضابطُ المعاكس قد ينقلب:** شرطُ «ليس تطبيقنا» **يُرضيه المحاكي الساقط** | ثلاثةُ ضوابطَ طبعت «مرّ» في اللحظة التي كان القياسُ فيها مستحيلاً |
| **صفرٌ من أداةٍ عمياء ليس نتيجة** ⇒ كلُّ «صفر» يلزمه **ضابطٌ موجب** | `strings` أعادت صفرَ سلسلةٍ من مانيفستٍ مقروء |
| **الحضورُ ليس اعتماداً** — يُقرأ سياقُ السلسلة لا وجودُها | `procorners.com` في الـdex: قائمةُ ثقةٍ لا وجهةُ طلب |
| **بسطٌ بلا مقام انطباعٌ لا قياس** — قارِن المعدّلات، **وتركُّزُ الحمل ليس تركُّزَ العطل** | ٩٩٪ من النداءات على مضيفٍ لا يعني أنه المعطوب |
| **الأنبوبُ يبتلع المخرَجَ ورمزَ الخروج** | `\| tail -6` أعطى صفراً — **دليلَ أنبوبٍ لا دليلَ تعليق** |
| **موردٌ ثقيلٌ بلا مُنهٍ تسريبٌ لا بقاء** | محاكٍ احتجز ٣٫٤٩ غ.ب بعد انتهاء غرضه |
| 🔴 **امتلاكُ الرقم ليس استخلاصَه** | البيانةُ في يدي (`yemenschoolz` غائبٌ من `vc31`) ولم أستخرج نتيجتَها |
| 🔴 **الدورُ لا يعصم: المنسّقُ ناقلٌ أيضاً** | لمتُ جلسةً على بناءِ بندٍ على ترجيحي، ثمّ فعلتُ المثلَ برقمٍ أوّليّ |

---

## §٤ · المفتوحُ — بمالكِ كلٍّ

### بمالكٍ `user` (قرارُ المالك أو أدواتٌ لا يملكها إلّا هو)

| البند | لماذا |
|---|---|
| 🔴 **ترقيةُ `production`** | ثلاثةُ شروطٍ متّفقٌ عليها: إشعارٌ حقيقيّ على جهاز · `FCM_PROJECT_ID` في `teacher` · وملاحظاتُ إصدار |
| 🔴 **`Master_Admin_School` مفتوحٌ للعموم بصلاحية `writer`** | يحمل معرّفاتِ ملفّاتٍ وبريدَ مديرين و`admin_password` |
| 🔴 **تقييدُ مفتاح Firebase بـSHA-1 في GCP** | الحمايةُ في التقييد لا في الإخفاء — **لم يُقَس** |
| **`ANDROID_LATEST_VERSION_CODE = 33`** | 🔴 **بعد `production` وحده** — قيمتُها الآن `0` ⇒ صفرُ بانرِ تحديث. ونطاقُها `SchoolApp-gas` |
| **عتبةُ `targetSdk` المطلوبة** | «≥36» **مشتقٌّ لا مقروء** من Play Console |
| **`Workspace\Releases\…\README.md`** | حجبه `paired-root-shim` بحقّ — يُطبَّق من جلسةٍ جذرُها `Workspace` |
| **عشرةُ فروعٍ محلّية قديمة** | ومنها 🔴 **`diag/ssl-error-logging`** — يشحن `Log.e("SSL ERROR DETAILS — url=…, cert=…")`. **لم أحذف شيئاً**: الحذفُ لا رجعةَ فيه |
| 🔴 **أصلُ المتجر يفقد الخدمة** | **١٨٫٥٪ 5xx** على ٣٬١٥٩ طلباً/٣ ساعات · `524`×١٠٢ بمتوسّط أصلٍ **١٢٥٬١٤٨ ms** · `499`×٣٠٥ ⇒ زوّارٌ غادروا. **تذكرةُ `sellvir`** |
| **خطّةُ كاش HTML للمتجر** | ⏸️ موقوفةٌ عمداً — تحتاج حذفَ `Set-Cookie`، **ولا تُجرَّب على أصلٍ مضطرب** |
| **دمجُ PR #10** في `rukn-tasawuq-erp` | مفتوحٌ بستّة كوميتات |
| 🔴 **`split-student-gas-deployment`** | **الرافعةُ البنيوية الوحيدة للـ502** · شقُّ الوركر سطرٌ جاهز، والحاجزُ في GAS وقرارِ المالك |
| **`standing-grant-stale-condition`** | 🔴 حاجزُه **غيابُ قدرةٍ لا انتظارُ إذن**: لا أداةَ في أيّ جلسة تخفض صلاحيةَ Drive |
| **`store-zone-origin-exposed-dns`** | تسعةُ سجلّاتٍ غيرِ مُوكَّلة تكشف الأصل |
| ~~ثغرةُ `/cart` مقابل `/cart/`~~ | 🟢 **أُغلقت** — الاستثناءُ صار `path in {"/cart" "/userlogin"}` + `starts_with` للباقي |

### بمالكٍ `claude`

| البند | الحاجز |
|---|---|
| **جذرُ سقوط رفعتَي Play** | متغيّران · **تجربتان تعزلانه** |
| **`API 24-30`** | 🟢 **ضاق ولم يُغلق**: لا يمسّ إلّا مَن يُثبّت `vc33` فأحدث — ويبقى لأن `minSdk = 24` |
| **وصولُ FCM على جهازٍ حقيقيّ** | يحتاج إرسالاً حيّاً + جهازاً |

---

## §٥ · المطالبةُ التالية — حرفياً

> افتح الجلسة من `C:\Users\osama\AndroidStudioProjects\SchoolAppyemen`، واقرأ
> `_docs/2026-09-01-تسليم-جلسة-vc33.md` ثمّ هذه الوثيقة. **`vc33` على `internal` ولم يبلغ
> مستخدماً؛ و`production` بانتظار المالك بشروطه الثلاثة.**
> 🔴 **ولا ترفع إلى Play بلا موافقةٍ لحظيةٍ من المالك في جلستك أنت** — ولا تصدّق مخرَجَ
> `deploy-to-play.ps1` وحدَه ولو خرج بصفر؛ **تحقّق من خارجه.**
> وأولُ ما يُنجَز: **عزلُ جذرِ سقوط رفعتَي Play** — متغيّران، وتجربتان تكفيان.

---

## ملحق · حدُّ المِلكية في جلستي

✅ **لُمس:** `SchoolAppyemen` وحده.
❌ **لم يُلمس:** الوركرُ و`assetlinks` · خصائصُ سكربت `teacher` ونشراتُ GAS · `pending.json`
ولوحةُ الصحّة · `Workspace\Releases\README.md` · `YemenSchoolz` · `applicationId` · مفتاحُ
التوقيع · `AppConfig.kt` · معرّفاتُ النشر.
