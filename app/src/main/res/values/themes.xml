<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Base application theme.
        No action bar — the app controls its own toolbar via WebView.
    -->
    <style name="Theme.SchoolAppYemen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorAccent">@color/brand_accent</item>
        <item name="android:windowBackground">@color/ic_launcher_background</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">false</item>
    </style>

    <!--
        Splash screen theme — required by AndroidX SplashScreen API.
        Applied ONLY to SplashActivity via android:theme in AndroidManifest.xml.

        windowSplashScreenBackground: the color shown behind the icon
        windowSplashScreenAnimatedIcon: the icon displayed (use foreground layer)
        postSplashScreenTheme: the theme applied AFTER the splash dismisses
    -->
    <style name="Theme.SchoolApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.AppCompat.Light.NoActionBar</item>
    </style>

</resources>